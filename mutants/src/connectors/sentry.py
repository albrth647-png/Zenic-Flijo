"""
Conector Sentry — Seguimiento de Errores via Sentry API
===========================================================

Permite gestionar errores, liberaciones, proyectos y
alertas via la Sentry API.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁSentryConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁ_get_token__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁ_list_issues__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁ_get_issue__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁ_create_release__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁ_list_releases__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁ_list_projects__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSentryConnectorǁ_get_event__mutmut: MutantDict = {}  # type: ignore


class SentryConnector(BaseConnector):
    """Conector para Sentry: errores, liberaciones y alertas."""

    name = "sentry"
    version = "1.0.0"
    description = "Gestiona errores, liberaciones y alertas via Sentry"
    category = "devops_monitoring"
    icon = "alert-circle"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://sentry.io/api/0"
        self._http: HttpClient | None = None

    def xǁSentryConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://sentry.io/api/0"
        self._http: HttpClient | None = None

    def xǁSentryConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁSentryConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://sentry.io/api/0XX"
        self._http: HttpClient | None = None

    def xǁSentryConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://SENTRY.IO/API/0"
        self._http: HttpClient | None = None

    def xǁSentryConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://sentry.io/api/0"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ_get_token__mutmut)
    def _get_token(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_orig(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_1(self) -> str:
        """Extract the auth token from the auth provider."""
        if self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_2(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return "XXXX"
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_3(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = None
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_4(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"XXheadersXX": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_5(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"HEADERS": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_6(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "XXparamsXX": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_7(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "PARAMS": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_8(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(None)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_9(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = None
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_10(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get(None, "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_11(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", None)
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_12(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_13(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", )
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_14(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["XXheadersXX"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_15(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["HEADERS"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_16(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("XXAuthorizationXX", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_17(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_18(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("AUTHORIZATION", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_19(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "XXXX")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_20(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith(None):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_21(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("XXBearer XX"):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_22(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_23(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("BEARER "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_24(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[8:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_25(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith(None):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_26(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("XXToken XX"):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_27(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_28(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("TOKEN "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_29(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[7:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_30(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = None
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_31(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get(None, "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_32(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", None)
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_33(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_34(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", )
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_35(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["XXheadersXX"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_36(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["HEADERS"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_37(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("XXX-API-KeyXX", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_38(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("x-api-key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_39(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-KEY", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_40(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "XXXX")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_41(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("XX_api_keyXX", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_42(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_API_KEY", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_43(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "XX_tokenXX", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_44(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_TOKEN", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_45(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "XX_access_tokenXX", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_46(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_ACCESS_TOKEN", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_47(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "XX_auth_tokenXX"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_48(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_AUTH_TOKEN"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_49(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(None, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_50(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, None):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_51(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_52(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, ):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_53(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = None
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_54(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(None, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_55(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, None, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_56(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, None)
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_57(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_58(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_59(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, )
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_60(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "XXXX")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_61(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) or val:
                    return val
        return ""

    def xǁSentryConnectorǁ_get_token__mutmut_62(self) -> str:
        """Extract the auth token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_auth_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return "XXXX"

    @_mutmut_mutated(mutants_xǁSentryConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la Sentry API."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXSentryConnector: Auth Token no configuradoXX")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("sentryconnector: auth token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SENTRYCONNECTOR: AUTH TOKEN NO CONFIGURADO")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return True

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = None
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error(None)
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("XXSentryConnector: No se pudo extraer el token del auth providerXX")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("sentryconnector: no se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SENTRYCONNECTOR: NO SE PUDO EXTRAER EL TOKEN DEL AUTH PROVIDER")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return True

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = None
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=None, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=None)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, )
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth(None, token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=None)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth(token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", )

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("XXBearerXX", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("BEARER", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = None
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get(None)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("XX/organizations/XX")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/ORGANIZATIONS/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = None
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = False
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation(None, "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", None)
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", )
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("XXconnectXX", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("CONNECT", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "XXAuth Token configurado y validadoXX")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "auth token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "AUTH TOKEN CONFIGURADO Y VALIDADO")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return False
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(None)
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = ""
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return True
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(None)
            self._http = None
            return False

    def xǁSentryConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = ""
            return False

    def xǁSentryConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la Sentry API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SentryConnector: Auth Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("SentryConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # Sentry uses Bearer token auth
        self._http.set_auth("Bearer", token=token)

        # Validate credentials by fetching organizations
        try:
            resp = self._http.get("/organizations/")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Auth Token configurado y validado")
                return True
            else:
                logger.error(f"SentryConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"SentryConnector: Error validando token - {e}")
            self._http = None
            return True

    @_mutmut_mutated(mutants_xǁSentryConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXlist_issuesXX": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "LIST_ISSUES": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "XXget_issueXX": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "GET_ISSUE": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "XXcreate_releaseXX": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "CREATE_RELEASE": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "XXlist_releasesXX": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "LIST_RELEASES": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "XXlist_projectsXX": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "LIST_PROJECTS": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "XXget_eventXX": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "GET_EVENT": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁSentryConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Sentry.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_issues": self._list_issues,
            "get_issue": self._get_issue,
            "create_release": self._create_release,
            "list_releases": self._list_releases,
            "list_projects": self._list_projects,
            "get_event": self._get_event,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁSentryConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el Auth Token de Sentry este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSentryConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el Auth Token de Sentry este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSentryConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el Auth Token de Sentry este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSentryConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el Auth Token de Sentry este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁSentryConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁSentryConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Sentry."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ_list_issues__mutmut)
    def _list_issues(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = None
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get(None, "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", None)
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", )
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("XXorganization_slugXX", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("ORGANIZATION_SLUG", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "XXXX")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = None
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get(None, "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", None)
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", )
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("XXproject_slugXX", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("PROJECT_SLUG", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "XXXX")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org and not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"SUCCESS": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": True, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "XXerrorXX": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "ERROR": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "XXParametros requeridos: organization_slug, project_slugXX"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: ORGANIZATION_SLUG, PROJECT_SLUG"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation(None, f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation(f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("XXlist_issuesXX", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("LIST_ISSUES", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = None
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"XXprojectXX": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"PROJECT": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("XXqueryXX", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("QUERY", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "XXsortXX", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "SORT", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "XXstatsPeriodXX", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsperiod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "STATSPERIOD", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "XXcursorXX", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "CURSOR", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "XXlimitXX", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "LIMIT", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "XXstartXX", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "START", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "XXendXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "END"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = None

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = None
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = None
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get(None, 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", None)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get(10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", )
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("XXmax_pagesXX", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("MAX_PAGES", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 11)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = None

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop(None, None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop(None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", )

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("XXcursorXX", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("CURSOR", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(None):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = None
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["XXcursorXX"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["CURSOR"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = None
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(None)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(None)

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get(None, []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", None))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get([]))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", ))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("XXdataXX", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("DATA", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = None
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get(None, "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", None)
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", )
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("XXLinkXX", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("LINK", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "XXXX")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = None
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(None)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "XXnextXX" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "NEXT" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    return

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = None
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get(None, "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", None)
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", )
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("XXnextXX", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("NEXT", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "XXXX")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "XXcursor=XX" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "CURSOR=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" not in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = None
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(None)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = None
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(None)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = None
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get(None, [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", None)[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get([None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", )[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("XXcursorXX", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("CURSOR", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[1]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    return

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "XXsuccessXX": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "SUCCESS": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": False,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "XXissuesXX": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "ISSUES": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": True, "error": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁSentryConnectorǁ_list_issues__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues (errores) de un proyecto de Sentry.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug', opcionalmente
                    'query', 'limit', 'cursor', 'sort', 'statsPeriod'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_issues", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("query", "sort", "statsPeriod", "cursor", "limit", "start", "end"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/issues/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_issues.extend(data)
                elif isinstance(data, dict):
                    all_issues.extend(data.get("data", []))

                # Sentry uses Link header for pagination
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "issues": all_issues,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_issues error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ_get_issue__mutmut)
    def _get_issue(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = None
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get(None, "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", None)
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", )
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("XXorganization_slugXX", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("ORGANIZATION_SLUG", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "XXXX")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = None
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get(None, "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", None)
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", )
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("XXissue_idXX", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("ISSUE_ID", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "XXXX")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org and not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"SUCCESS": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": True, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "XXerrorXX": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "ERROR": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "XXParametros requeridos: organization_slug, issue_idXX"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: ORGANIZATION_SLUG, ISSUE_ID"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation(None, f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation(f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("XXget_issueXX", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("GET_ISSUE", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = None
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(None)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "XXsuccessXX": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "SUCCESS": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": True,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "XXerrorXX": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "ERROR": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXidXX": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "ID": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get(None, issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", None),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get(issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", ),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("XXidXX", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("ID", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "XXtitleXX": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "TITLE": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get(None, ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", None),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get(""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("XXtitleXX", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("TITLE", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", "XXXX"),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "XXstatusXX": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "STATUS": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get(None, "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", None),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", ),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("XXstatusXX", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("STATUS", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "XXunresolvedXX"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "UNRESOLVED"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "XXlevelXX": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "LEVEL": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get(None, ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", None),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get(""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("XXlevelXX", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("LEVEL", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", "XXXX"),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "XXcountXX": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "COUNT": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get(None, 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", None),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get(0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", ),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("XXcountXX", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("COUNT", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 1),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "XXuserCountXX": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "usercount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "USERCOUNT": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get(None, 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", None),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get(0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", ),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("XXuserCountXX", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("usercount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("USERCOUNT", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 1),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "XXfirstSeenXX": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstseen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "FIRSTSEEN": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get(None, ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", None),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get(""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("XXfirstSeenXX", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstseen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("FIRSTSEEN", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", "XXXX"),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "XXlastSeenXX": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastseen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "LASTSEEN": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get(None, ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", None),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get(""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("XXlastSeenXX", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastseen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("LASTSEEN", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", "XXXX"),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "XXplatformXX": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "PLATFORM": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get(None, ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", None),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get(""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("XXplatformXX", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("PLATFORM", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", "XXXX"),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "XXprojectXX": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "PROJECT": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get(None, "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", None) if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", ) if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get(None, {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", None).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get({}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", ).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("XXprojectXX", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("PROJECT", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("XXslugXX", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("SLUG", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "XXXX") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "XXXX",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "XXtypeXX": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "TYPE": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get(None, ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", None),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get(""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("XXtypeXX", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("TYPE", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", "XXXX"),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "XXshortIdXX": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortid": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "SHORTID": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get(None, ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", None),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get(""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("XXshortIdXX", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortid", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("SHORTID", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", "XXXX"),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "XXpermalinkXX": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "PERMALINK": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get(None, ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get(""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("XXpermalinkXX", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("PERMALINK", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", "XXXX"),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": True, "error": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁSentryConnectorǁ_get_issue__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un issue (error) por su ID.

        Args:
            params: Debe contener 'organization_slug' y 'issue_id'
        """
        org = params.get("organization_slug", "")
        issue_id = params.get("issue_id", "")
        if not org or not issue_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, issue_id"}
        self._log_operation("get_issue", f"org={org}, issue={issue_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/organizations/{org}/issues/{issue_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", issue_id),
                    "title": data.get("title", ""),
                    "status": data.get("status", "unresolved"),
                    "level": data.get("level", ""),
                    "count": data.get("count", 0),
                    "userCount": data.get("userCount", 0),
                    "firstSeen": data.get("firstSeen", ""),
                    "lastSeen": data.get("lastSeen", ""),
                    "platform": data.get("platform", ""),
                    "project": data.get("project", {}).get("slug", "") if isinstance(data.get("project"), dict) else "",
                    "type": data.get("type", ""),
                    "shortId": data.get("shortId", ""),
                    "permalink": data.get("permalink", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_issue error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ_create_release__mutmut)
    def _create_release(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = None
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get(None, "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", None)
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", )
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("XXorganization_slugXX", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("ORGANIZATION_SLUG", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "XXXX")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = None
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get(None, "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", None)
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", )
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("XXversionXX", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("VERSION", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "XXXX")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = None
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get(None, [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", None)
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get([])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", )
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("XXprojectsXX", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("PROJECTS", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version and not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org and not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"SUCCESS": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": True, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "XXerrorXX": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "ERROR": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "XXParametros requeridos: organization_slug, version, projectsXX"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: ORGANIZATION_SLUG, VERSION, PROJECTS"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation(None, f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation(f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("XXcreate_releaseXX", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("CREATE_RELEASE", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = None
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"XXversionXX": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"VERSION": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "XXprojectsXX": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "PROJECTS": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("XXrefsXX", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("REFS", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "XXcommitCountXX", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitcount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "COMMITCOUNT", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "XXurlXX", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "URL", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "XXdateReleasedXX", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "datereleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "DATERELEASED", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "XXdateStartedXX"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "datestarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "DATESTARTED"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key not in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = None

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = None
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(None, json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=None)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", )
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "XXsuccessXX": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "SUCCESS": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": True,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "XXerrorXX": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "ERROR": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXversionXX": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "VERSION": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get(None, version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", None),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get(version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", ),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("XXversionXX", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("VERSION", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "XXprojectsXX": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "PROJECTS": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get(None, projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", None),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get(projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", ),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("XXprojectsXX", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("PROJECTS", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "XXdateCreatedXX": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "datecreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "DATECREATED": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get(None, ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", None),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get(""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("XXdateCreatedXX", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("datecreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("DATECREATED", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", "XXXX"),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "XXdateReleasedXX": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "datereleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "DATERELEASED": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get(None),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("XXdateReleasedXX"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("datereleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("DATERELEASED"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "XXurlXX": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "URL": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get(None, ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", None),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get(""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("XXurlXX", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("URL", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", "XXXX"),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "XXshortVersionXX": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortversion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "SHORTVERSION": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get(None, ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", None),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get(""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("XXshortVersionXX", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortversion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("SHORTVERSION", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", "XXXX"),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "XXauthorXX": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "AUTHOR": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get(None, {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", None),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get({}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", ),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("XXauthorXX", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("AUTHOR", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "XXcommitCountXX": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitcount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "COMMITCOUNT": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get(None, 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get(0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("XXcommitCountXX", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitcount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("COMMITCOUNT", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 1),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": True, "error": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁSentryConnectorǁ_create_release__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una liberacion en Sentry.

        Args:
            params: Debe contener 'organization_slug', 'version' y 'projects' (lista)
                    opcionalmente 'refs', 'commitCount', 'url', 'dateReleased'
        """
        org = params.get("organization_slug", "")
        version = params.get("version", "")
        projects = params.get("projects", [])
        if not org or not version or not projects:
            return {"success": False, "error": "Parametros requeridos: organization_slug, version, projects"}
        self._log_operation("create_release", f"org={org}, version={version}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"version": version, "projects": projects}
        for key in ("refs", "commitCount", "url", "dateReleased", "dateStarted"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/organizations/{org}/releases/", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "version": data.get("version", version),
                    "projects": data.get("projects", projects),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReleased": data.get("dateReleased"),
                    "url": data.get("url", ""),
                    "shortVersion": data.get("shortVersion", ""),
                    "author": data.get("author", {}),
                    "commitCount": data.get("commitCount", 0),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector create_release error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ_list_releases__mutmut)
    def _list_releases(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = None
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get(None, "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", None)
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", )
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("XXorganization_slugXX", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("ORGANIZATION_SLUG", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "XXXX")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = None
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get(None, "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", None)
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", )
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("XXproject_slugXX", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("PROJECT_SLUG", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "XXXX")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org and not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"SUCCESS": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": True, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "XXerrorXX": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "ERROR": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "XXParametros requeridos: organization_slug, project_slugXX"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: ORGANIZATION_SLUG, PROJECT_SLUG"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation(None, f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation(f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("XXlist_releasesXX", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("LIST_RELEASES", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = None
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"XXprojectXX": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"PROJECT": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("XXcursorXX", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("CURSOR", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "XXlimitXX", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "LIMIT", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "XXqueryXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "QUERY"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = None

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = None
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = None
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get(None, 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", None)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get(10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", )
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("XXmax_pagesXX", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("MAX_PAGES", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 11)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = None

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop(None, None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop(None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", )

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("XXcursorXX", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("CURSOR", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(None):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = None
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["XXcursorXX"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["CURSOR"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = None
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(None)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(None)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get(None, []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", None))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get([]))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", ))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("XXdataXX", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("DATA", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = None
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get(None, "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", None)
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", )
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("XXLinkXX", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("LINK", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "XXXX")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = None
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(None)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "XXnextXX" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "NEXT" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    return

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = None
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get(None, "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", None)
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", )
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("XXnextXX", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("NEXT", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "XXXX")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "XXcursor=XX" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "CURSOR=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" not in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = None
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(None)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = None
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(None)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = None
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get(None, [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", None)[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get([None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", )[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("XXcursorXX", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("CURSOR", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[1]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    return

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "XXsuccessXX": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "SUCCESS": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": False,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "XXreleasesXX": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "RELEASES": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": True, "error": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁSentryConnectorǁ_list_releases__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista liberaciones de un proyecto.

        Args:
            params: Debe contener 'organization_slug' y 'project_slug'
                    opcionalmente 'cursor', 'limit'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        if not org or not project:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug"}
        self._log_operation("list_releases", f"org={org}, project={project}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {"project": project}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_releases: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/releases/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_releases.extend(data)
                elif isinstance(data, dict):
                    all_releases.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "releases": all_releases,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_releases error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ_list_projects__mutmut)
    def _list_projects(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = None
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get(None, "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", None)
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", )
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("XXorganization_slugXX", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("ORGANIZATION_SLUG", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "XXXX")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"XXsuccessXX": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"SUCCESS": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": True, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "XXerrorXX": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "ERROR": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "XXParametro requerido: organization_slugXX"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "PARAMETRO REQUERIDO: ORGANIZATION_SLUG"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation(None, f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation(f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("XXlist_projectsXX", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("LIST_PROJECTS", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = None
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("XXcursorXX", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("CURSOR", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "XXlimitXX", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "LIMIT", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "XXqueryXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "QUERY"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = None

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = None
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = None
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get(None, 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", None)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get(10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", )
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("XXmax_pagesXX", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("MAX_PAGES", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 11)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = None

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop(None, None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop(None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", )

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("XXcursorXX", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("CURSOR", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(None):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = None
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["XXcursorXX"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["CURSOR"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = None
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(None)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(None)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get(None, []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", None))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get([]))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", ))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("XXdataXX", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("DATA", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = None
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get(None, "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", None)
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", )
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("XXLinkXX", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("LINK", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "XXXX")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = None
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(None)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "XXnextXX" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "NEXT" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    return

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = None
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get(None, "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", None)
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", )
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("XXnextXX", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("NEXT", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "XXXX")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "XXcursor=XX" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "CURSOR=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" not in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = None
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(None)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = None
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(None)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = None
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get(None, [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", None)[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get([None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", )[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("XXcursorXX", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("CURSOR", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[1]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    return

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "XXsuccessXX": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "SUCCESS": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": False,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "XXprojectsXX": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "PROJECTS": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": True, "error": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁSentryConnectorǁ_list_projects__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de una organizacion en Sentry.

        Args:
            params: Debe contener 'organization_slug'
                    opcionalmente 'cursor', 'limit', 'query'
        """
        org = params.get("organization_slug", "")
        if not org:
            return {"success": False, "error": "Parametro requerido: organization_slug"}
        self._log_operation("list_projects", f"org={org}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("cursor", "limit", "query"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            max_pages = params.get("max_pages", 10)
            cursor = query_params.pop("cursor", None)

            for _ in range(max_pages):
                if cursor:
                    query_params["cursor"] = cursor
                resp = self._http.get(f"/organizations/{org}/projects/", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"Sentry API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                data = resp.json()
                if isinstance(data, list):
                    all_projects.extend(data)
                elif isinstance(data, dict):
                    all_projects.extend(data.get("data", []))

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

                # Extract cursor from next URL
                next_url = links.get("next", "")
                if "cursor=" in next_url:
                    import urllib.parse
                    parsed = urllib.parse.urlparse(next_url)
                    qs = urllib.parse.parse_qs(parsed.query)
                    cursor = qs.get("cursor", [None])[0]
                else:
                    break

            return {
                "success": True,
                "projects": all_projects,
            }

        except HTTPClientError as e:
            logger.error(f"SentryConnector list_projects error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁSentryConnectorǁ_get_event__mutmut)
    def _get_event(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = None
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get(None, "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", None)
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", )
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("XXorganization_slugXX", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("ORGANIZATION_SLUG", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "XXXX")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = None
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get(None, "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", None)
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", )
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("XXproject_slugXX", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("PROJECT_SLUG", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "XXXX")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = None
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get(None, "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", None)
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", )
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("XXevent_idXX", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("EVENT_ID", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "XXXX")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project and not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org and not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"SUCCESS": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": True, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "XXerrorXX": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "ERROR": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "XXParametros requeridos: organization_slug, project_slug, event_idXX"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: ORGANIZATION_SLUG, PROJECT_SLUG, EVENT_ID"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation(None, f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation(f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("XXget_eventXX", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("GET_EVENT", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = None
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(None)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "XXsuccessXX": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "SUCCESS": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": True,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "XXerrorXX": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "ERROR": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXidXX": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "ID": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get(None, event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", None),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get(event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", ),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("XXidXX", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("ID", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "XXeventIDXX": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventid": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "EVENTID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get(None, ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", None),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get(""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("XXeventIDXX", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventid", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("EVENTID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", "XXXX"),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "XXmessageXX": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "MESSAGE": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get(None, ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", None),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get(""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("XXmessageXX", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("MESSAGE", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", "XXXX"),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "XXtitleXX": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "TITLE": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get(None, ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", None),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get(""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("XXtitleXX", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("TITLE", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", "XXXX"),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "XXtagsXX": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "TAGS": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get(None, []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", None),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get([]),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", ),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("XXtagsXX", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("TAGS", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "XXcontextsXX": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "CONTEXTS": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get(None, {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", None),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get({}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", ),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("XXcontextsXX", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("CONTEXTS", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "XXdateCreatedXX": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "datecreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "DATECREATED": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get(None, ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", None),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get(""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("XXdateCreatedXX", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("datecreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("DATECREATED", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", "XXXX"),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "XXdateReceivedXX": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "datereceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "DATERECEIVED": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get(None, ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", None),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get(""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("XXdateReceivedXX", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("datereceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("DATERECEIVED", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", "XXXX"),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "XXlevelXX": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "LEVEL": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get(None, ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", None),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get(""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("XXlevelXX", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("LEVEL", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", "XXXX"),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "XXplatformXX": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "PLATFORM": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get(None, ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", None),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get(""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("XXplatformXX", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("PLATFORM", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", "XXXX"),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "XXenvironmentXX": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "ENVIRONMENT": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get(None, ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", None),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get(""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("XXenvironmentXX", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("ENVIRONMENT", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", "XXXX"),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "XXreleaseXX": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "RELEASE": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get(None, ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", None),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get(""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("XXreleaseXX", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("RELEASE", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", "XXXX"),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "XXuserXX": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "USER": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get(None, {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", None),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get({}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", ),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("XXuserXX", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("USER", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "XXentriesXX": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "ENTRIES": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get(None, []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get([]),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("XXentriesXX", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("ENTRIES", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": True, "error": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁSentryConnectorǁ_get_event__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un evento especifico de Sentry.

        Args:
            params: Debe contener 'organization_slug', 'project_slug' y 'event_id'
        """
        org = params.get("organization_slug", "")
        project = params.get("project_slug", "")
        event_id = params.get("event_id", "")
        if not org or not project or not event_id:
            return {"success": False, "error": "Parametros requeridos: organization_slug, project_slug, event_id"}
        self._log_operation("get_event", f"event={event_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        try:
            resp = self._http.get(f"/projects/{org}/{project}/events/{event_id}/")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"Sentry API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id", event_id),
                    "eventID": data.get("eventID", ""),
                    "message": data.get("message", ""),
                    "title": data.get("title", ""),
                    "tags": data.get("tags", []),
                    "contexts": data.get("contexts", {}),
                    "dateCreated": data.get("dateCreated", ""),
                    "dateReceived": data.get("dateReceived", ""),
                    "level": data.get("level", ""),
                    "platform": data.get("platform", ""),
                    "environment": data.get("environment", ""),
                    "release": data.get("release", ""),
                    "user": data.get("user", {}),
                    "entries": data.get("entries", []),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"SentryConnector get_event error: {e}")
            return {"success": False, "error": str(None)}

mutants_xǁSentryConnectorǁ__init____mutmut['_mutmut_orig'] = SentryConnector.xǁSentryConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ__init____mutmut['xǁSentryConnectorǁ__init____mutmut_1'] = SentryConnector.xǁSentryConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ__init____mutmut['xǁSentryConnectorǁ__init____mutmut_2'] = SentryConnector.xǁSentryConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ__init____mutmut['xǁSentryConnectorǁ__init____mutmut_3'] = SentryConnector.xǁSentryConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ__init____mutmut['xǁSentryConnectorǁ__init____mutmut_4'] = SentryConnector.xǁSentryConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁSentryConnectorǁ_get_token__mutmut['_mutmut_orig'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_orig # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_1'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_1 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_2'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_2 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_3'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_3 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_4'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_4 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_5'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_5 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_6'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_6 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_7'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_7 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_8'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_8 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_9'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_9 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_10'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_10 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_11'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_11 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_12'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_12 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_13'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_13 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_14'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_14 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_15'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_15 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_16'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_16 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_17'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_17 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_18'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_18 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_19'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_19 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_20'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_20 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_21'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_21 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_22'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_22 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_23'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_23 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_24'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_24 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_25'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_25 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_26'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_26 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_27'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_27 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_28'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_28 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_29'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_29 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_30'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_30 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_31'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_31 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_32'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_32 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_33'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_33 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_34'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_34 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_35'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_35 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_36'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_36 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_37'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_37 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_38'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_38 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_39'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_39 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_40'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_40 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_41'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_41 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_42'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_42 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_43'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_43 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_44'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_44 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_45'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_45 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_46'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_46 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_47'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_47 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_48'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_48 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_49'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_49 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_50'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_50 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_51'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_51 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_52'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_52 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_53'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_53 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_54'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_54 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_55'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_55 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_56'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_56 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_57'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_57 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_58'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_58 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_59'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_59 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_60'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_60 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_61'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_61 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁ_get_token__mutmut['xǁSentryConnectorǁ_get_token__mutmut_62'] = SentryConnector.xǁSentryConnectorǁ_get_token__mutmut_62 # type: ignore # mutmut generated

mutants_xǁSentryConnectorǁconnect__mutmut['_mutmut_orig'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_1'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_2'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_3'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_4'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_5'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_6'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_7'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_8'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_9'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_10'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_11'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_12'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_13'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_14'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_15'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_16'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_17'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut_18'] = SentryConnector.xǁSentryConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁSentryConnectorǁconnect__mutmut['xǁSentryConnectorǁconnect__mutmut