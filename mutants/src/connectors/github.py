"""
Conector GitHub — Repos, Issues y PRs via GitHub API
========================================================

Permite gestionar repositorios, issues, pull requests,
acciones y busqueda via la GitHub REST API.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁGithubConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁ_get_token__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁ_list_repos__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁ_create_issue__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁ_list_issues__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁ_create_pull_request__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁ_list_pull_requests__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGithubConnectorǁ_search__mutmut: MutantDict = {}  # type: ignore


class GithubConnector(BaseConnector):
    """Conector para GitHub: repos, issues, PRs y acciones."""

    name = "github"
    version = "1.0.0"
    description = "Gestiona repositorios, issues y pull requests via GitHub API"
    category = "devops_monitoring"
    icon = "git-branch"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.github.com"
        self._http: HttpClient | None = None

    def xǁGithubConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.github.com"
        self._http: HttpClient | None = None

    def xǁGithubConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁGithubConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://api.github.comXX"
        self._http: HttpClient | None = None

    def xǁGithubConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://API.GITHUB.COM"
        self._http: HttpClient | None = None

    def xǁGithubConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.github.com"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ_get_token__mutmut)
    def _get_token(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_orig(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_1(self) -> str:
        """Extract the API token from the auth provider."""
        if self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_2(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return "XXXX"
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_3(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = None
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_4(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"XXheadersXX": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_5(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"HEADERS": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_6(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "XXparamsXX": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_7(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "PARAMS": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_8(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(None)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_9(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = None
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_10(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get(None, "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_11(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", None)
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_12(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_13(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", )
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_14(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["XXheadersXX"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_15(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["HEADERS"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_16(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("XXAuthorizationXX", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_17(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_18(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("AUTHORIZATION", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_19(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "XXXX")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_20(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith(None):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_21(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("XXBearer XX"):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_22(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_23(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("BEARER "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_24(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[8:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_25(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith(None):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_26(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("XXToken XX"):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_27(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_28(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("TOKEN "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_29(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[7:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_30(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith(None):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_31(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("XXBasic XX"):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_32(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_33(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("BASIC "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_34(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = None
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_35(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get(None, "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_36(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", None)
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_37(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_38(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", )
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_39(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["XXheadersXX"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_40(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["HEADERS"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_41(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("XXX-API-KeyXX", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_42(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("x-api-key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_43(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-KEY", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_44(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "XXXX")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_45(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("XX_api_keyXX", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_46(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_API_KEY", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_47(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "XX_tokenXX", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_48(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_TOKEN", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_49(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "XX_access_tokenXX"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_50(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_ACCESS_TOKEN"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_51(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(None, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_52(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, None):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_53(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_54(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, ):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_55(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = None
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_56(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(None, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_57(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, None, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_58(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, None)
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_59(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_60(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_61(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, )
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_62(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "XXXX")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_63(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) or val:
                    return val
        return ""

    def xǁGithubConnectorǁ_get_token__mutmut_64(self) -> str:
        """Extract the API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        if auth_header.startswith("Basic "):
            return auth_header  # Use full Basic header
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access for common patterns
        for attr in ("_api_key", "_token", "_access_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return "XXXX"

    @_mutmut_mutated(mutants_xǁGithubConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la GitHub API."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXGithubConnector: Token no configuradoXX")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("githubconnector: token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GITHUBCONNECTOR: TOKEN NO CONFIGURADO")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return True

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = None
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error(None)
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("XXGithubConnector: No se pudo extraer el token del auth providerXX")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("githubconnector: no se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GITHUBCONNECTOR: NO SE PUDO EXTRAER EL TOKEN DEL AUTH PROVIDER")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return True

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = None
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=None, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=None)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, )
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header(None, f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", None)
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header(f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", )
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("XXAuthorizationXX", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("AUTHORIZATION", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header(None, "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", None)

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", )

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("XXAcceptXX", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("ACCEPT", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "XXapplication/vnd.github+jsonXX")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "APPLICATION/VND.GITHUB+JSON")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = None
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get(None)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("XX/userXX")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/USER")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = None
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = False
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation(None, "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", None)
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", )
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("XXconnectXX", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("CONNECT", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "XXToken configurado y validadoXX")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "TOKEN CONFIGURADO Y VALIDADO")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return False
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(None)
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = ""
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return True
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(None)
            self._http = None
            return False

    def xǁGithubConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = ""
            return False

    def xǁGithubConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la GitHub API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GithubConnector: Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GithubConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitHub uses token auth (not Bearer): Authorization: token <token>
        self._http.set_header("Authorization", f"token {token}")
        self._http.set_header("Accept", "application/vnd.github+json")

        # Validate credentials by fetching authenticated user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Token configurado y validado")
                return True
            else:
                logger.error(f"GithubConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GithubConnector: Error validando token - {e}")
            self._http = None
            return True

    @_mutmut_mutated(mutants_xǁGithubConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXlist_reposXX": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "LIST_REPOS": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "XXcreate_issueXX": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "CREATE_ISSUE": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "XXlist_issuesXX": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "LIST_ISSUES": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "XXcreate_pull_requestXX": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "CREATE_PULL_REQUEST": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "XXlist_pull_requestsXX": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "LIST_PULL_REQUESTS": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "XXsearchXX": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "SEARCH": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁGithubConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitHub.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_repos": self._list_repos,
            "create_issue": self._create_issue,
            "list_issues": self._list_issues,
            "create_pull_request": self._create_pull_request,
            "list_pull_requests": self._list_pull_requests,
            "search": self._search,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁGithubConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el Token de GitHub este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGithubConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el Token de GitHub este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGithubConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el Token de GitHub este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGithubConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el Token de GitHub este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁGithubConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁGithubConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con GitHub."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ_list_repos__mutmut)
    def _list_repos(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation(None, f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", None)
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation(f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", )
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("XXlist_reposXX", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("LIST_REPOS", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get(None, 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', None)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get(30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', )}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('XXper_pageXX', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('PER_PAGE', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 31)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = None
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("XXtypeXX", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("TYPE", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "XXsortXX", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "SORT", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "XXdirectionXX", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "DIRECTION", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "XXper_pageXX", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "PER_PAGE", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "XXpageXX", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "PAGE", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "XXsinceXX", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "SINCE", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "XXvisibilityXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "VISIBILITY"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = None

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = None
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = None
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get(None, 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", None)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get(1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", )
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("XXpageXX", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("PAGE", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 2)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = None
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(None, 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", None)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", )
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("XXper_pageXX", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("PER_PAGE", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 31)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = None

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get(None, 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", None)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get(10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", )

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("XXmax_pagesXX", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("MAX_PAGES", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 11)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(None, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, None):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, ):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page - max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = None
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["XXpageXX"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["PAGE"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = None
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["XXper_pageXX"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["PER_PAGE"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("XX/user/reposXX", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/USER/REPOS", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = None
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) and len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) != 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 1:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    return

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(None)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = None
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get(None, "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", None)
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", )
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("XXLinkXX", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("LINK", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "XXXX")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = None
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(None)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "XXnextXX" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "NEXT" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    return

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "XXsuccessXX": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "SUCCESS": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": False,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "XXreposXX": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "REPOS": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "XXtotal_countXX": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "TOTAL_COUNT": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGithubConnectorǁ_list_repos__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista repositorios del usuario autenticado.

        Args:
            params: Opcionalmente 'type' (all/owner/member), 'sort', 'per_page', 'page'
        """
        self._log_operation("list_repos", f"per_page={params.get('per_page', 30)}")
        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("type", "sort", "direction", "per_page", "page", "since", "visibility"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_repos: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/user/repos", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                repos = resp.json()
                if not isinstance(repos, list) or len(repos) == 0:
                    break

                all_repos.extend(repos)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "repos": all_repos,
                "total_count": len(all_repos),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_repos error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ_create_issue__mutmut)
    def _create_issue(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = None
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get(None, "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", None)
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", )
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("XXownerXX", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("OWNER", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "XXXX")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = None
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get(None, "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", None)
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", )
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("XXrepoXX", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("REPO", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "XXXX")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = None
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get(None, "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", None)
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", )
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("XXtitleXX", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("TITLE", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "XXXX")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo and not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner and not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"SUCCESS": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": True, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "XXerrorXX": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "ERROR": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "XXParametros requeridos: owner, repo, titleXX"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: OWNER, REPO, TITLE"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation(None, f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation(f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("XXcreate_issueXX", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("CREATE_ISSUE", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = None
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"XXtitleXX": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"TITLE": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("XXbodyXX", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("BODY", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "XXlabelsXX", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "LABELS", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "XXassigneesXX", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "ASSIGNEES", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "XXmilestoneXX", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "MILESTONE", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "XXstateXX"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "STATE"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key not in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = None

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = None
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(None, json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=None)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", )
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = None
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "XXUnknown errorXX"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "UNKNOWN ERROR"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "XXsuccessXX": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "SUCCESS": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": True,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "XXerrorXX": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "ERROR": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "XXdetailsXX": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "DETAILS": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXnumberXX": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "NUMBER": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get(None),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("XXnumberXX"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("NUMBER"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "XXtitleXX": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "TITLE": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get(None, title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", None),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get(title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", ),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("XXtitleXX", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("TITLE", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "XXstateXX": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "STATE": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get(None, "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", None),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", ),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("XXstateXX", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("STATE", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "XXopenXX"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "OPEN"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "XXhtml_urlXX": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "HTML_URL": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get(None, ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", None),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get(""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("XXhtml_urlXX", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("HTML_URL", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", "XXXX"),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "CREATED_AT": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get(None, ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", None),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get(""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("CREATED_AT", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", "XXXX"),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "XXuserXX": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "USER": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get(None, ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get(""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get(None, {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", None).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get({}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", ).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("XXuserXX", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("USER", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("XXloginXX", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("LOGIN", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", "XXXX"),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGithubConnectorǁ_create_issue__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un issue en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title' y opcionalmente 'body', 'labels', 'assignees'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        if not owner or not repo or not title:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title"}
        self._log_operation("create_issue", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title}
        for key in ("body", "labels", "assignees", "milestone", "state"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/issues", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "user": data.get("user", {}).get("login", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_issue error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ_list_issues__mutmut)
    def _list_issues(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = None
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get(None, "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", None)
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", )
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("XXownerXX", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("OWNER", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "XXXX")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = None
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get(None, "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", None)
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", )
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("XXrepoXX", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("REPO", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "XXXX")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner and not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"SUCCESS": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": True, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "XXerrorXX": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "ERROR": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "XXParametros requeridos: owner, repoXX"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: OWNER, REPO"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation(None, f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation(f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("XXlist_issuesXX", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("LIST_ISSUES", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = None
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("XXstateXX", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("STATE", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "XXlabelsXX", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "LABELS", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "XXsortXX", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "SORT", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "XXdirectionXX", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "DIRECTION", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "XXsinceXX", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "SINCE", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "XXper_pageXX", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "PER_PAGE", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "XXpageXX", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "PAGE", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "XXmilestoneXX", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "MILESTONE", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "XXassigneeXX", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "ASSIGNEE", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "XXcreatorXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "CREATOR"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = None

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = None
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = None
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get(None, 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", None)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get(1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", )
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("XXpageXX", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("PAGE", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 2)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = None
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(None, 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", None)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", )
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("XXper_pageXX", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("PER_PAGE", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 31)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = None

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get(None, 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", None)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get(10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", )

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("XXmax_pagesXX", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("MAX_PAGES", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 11)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(None, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, None):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, ):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page - max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = None
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["XXpageXX"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["PAGE"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = None
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["XXper_pageXX"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["PER_PAGE"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = None
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) and len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) != 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 1:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    return

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(None)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = None
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get(None, "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", None)
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", )
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("XXLinkXX", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("LINK", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "XXXX")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = None
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(None)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "XXnextXX" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "NEXT" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    return

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "XXsuccessXX": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "SUCCESS": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": False,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "XXissuesXX": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "ISSUES": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "XXtotal_countXX": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "TOTAL_COUNT": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGithubConnectorǁ_list_issues__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista issues de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'labels', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_issues", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "labels", "sort", "direction", "since", "per_page", "page", "milestone", "assignee", "creator"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_issues: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/issues", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                issues = resp.json()
                if not isinstance(issues, list) or len(issues) == 0:
                    break

                all_issues.extend(issues)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "issues": all_issues,
                "total_count": len(all_issues),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_issues error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ_create_pull_request__mutmut)
    def _create_pull_request(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = None
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get(None, "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", None)
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", )
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("XXownerXX", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("OWNER", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "XXXX")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = None
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get(None, "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", None)
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", )
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("XXrepoXX", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("REPO", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "XXXX")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = None
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get(None, "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", None)
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", )
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("XXtitleXX", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("TITLE", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "XXXX")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = None
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get(None, "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", None)
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", )
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("XXheadXX", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("HEAD", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "XXXX")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = None
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get(None, "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", None)
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", )
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("XXbaseXX", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("BASE", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "XXmainXX")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "MAIN")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title and not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo and not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner and not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"SUCCESS": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": True, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "XXerrorXX": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "ERROR": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "XXParametros requeridos: owner, repo, title, headXX"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: OWNER, REPO, TITLE, HEAD"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation(None, f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation(f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("XXcreate_pull_requestXX", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("CREATE_PULL_REQUEST", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = None
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"XXtitleXX": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"TITLE": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "XXheadXX": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "HEAD": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "XXbaseXX": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "BASE": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "XXbodyXX" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "BODY" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" not in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = None
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["XXbodyXX"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["BODY"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["XXbodyXX"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["BODY"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "XXdraftXX" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "DRAFT" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" not in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = None

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["XXdraftXX"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["DRAFT"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["XXdraftXX"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["DRAFT"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = None
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(None, json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=None)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", )
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = None
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "XXUnknown errorXX"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "UNKNOWN ERROR"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "XXsuccessXX": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "SUCCESS": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": True,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "XXerrorXX": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "ERROR": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "XXdetailsXX": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "DETAILS": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXnumberXX": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "NUMBER": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get(None),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("XXnumberXX"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("NUMBER"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "XXtitleXX": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "TITLE": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get(None, title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", None),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get(title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", ),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("XXtitleXX", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("TITLE", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "XXstateXX": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "STATE": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get(None, "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", None),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", ),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("XXstateXX", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("STATE", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "XXopenXX"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "OPEN"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "XXhtml_urlXX": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "HTML_URL": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get(None, ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", None),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get(""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("XXhtml_urlXX", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("HTML_URL", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", "XXXX"),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "CREATED_AT": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get(None, ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", None),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get(""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("CREATED_AT", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", "XXXX"),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "XXheadXX": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "HEAD": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get(None, head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", None),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get(head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", ),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get(None, {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", None).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get({}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", ).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("XXheadXX", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("HEAD", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("XXrefXX", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("REF", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "XXbaseXX": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "BASE": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get(None, base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get(base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get(None, {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", None).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get({}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", ).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("XXbaseXX", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("BASE", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("XXrefXX", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("REF", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGithubConnectorǁ_create_pull_request__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un pull request en un repositorio.

        Args:
            params: Debe contener 'owner', 'repo', 'title', 'head', 'base' y opcionalmente 'body'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        title = params.get("title", "")
        head = params.get("head", "")
        base_branch = params.get("base", "main")
        if not owner or not repo or not title or not head:
            return {"success": False, "error": "Parametros requeridos: owner, repo, title, head"}
        self._log_operation("create_pull_request", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        body: dict[str, Any] = {"title": title, "head": head, "base": base_branch}
        if "body" in params:
            body["body"] = params["body"]
        if "draft" in params:
            body["draft"] = params["draft"]

        try:
            resp = self._http.post(f"/repos/{owner}/{repo}/pulls", json=body)
            if not resp.ok:
                error_detail = resp.body if isinstance(resp.body, (dict, str)) else "Unknown error"
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": error_detail,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "number": data.get("number"),
                    "title": data.get("title", title),
                    "state": data.get("state", "open"),
                    "html_url": data.get("html_url", ""),
                    "created_at": data.get("created_at", ""),
                    "head": data.get("head", {}).get("ref", head),
                    "base": data.get("base", {}).get("ref", base_branch),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector create_pull_request error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ_list_pull_requests__mutmut)
    def _list_pull_requests(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = None
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get(None, "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", None)
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", )
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("XXownerXX", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("OWNER", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "XXXX")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = None
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get(None, "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", None)
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", )
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("XXrepoXX", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("REPO", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "XXXX")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner and not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"SUCCESS": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": True, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "XXerrorXX": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "ERROR": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "XXParametros requeridos: owner, repoXX"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: OWNER, REPO"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation(None, f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation(f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("XXlist_pull_requestsXX", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("LIST_PULL_REQUESTS", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = None
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("XXstateXX", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("STATE", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "XXheadXX", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "HEAD", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "XXbaseXX", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "BASE", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "XXsortXX", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "SORT", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "XXdirectionXX", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "DIRECTION", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "XXper_pageXX", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "PER_PAGE", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "XXpageXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "PAGE"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = None

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = None
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = None
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get(None, 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", None)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get(1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", )
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("XXpageXX", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("PAGE", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 2)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = None
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(None, 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", None)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", )
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("XXper_pageXX", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("PER_PAGE", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 31)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = None

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get(None, 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", None)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get(10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", )

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("XXmax_pagesXX", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("MAX_PAGES", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 11)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(None, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, None):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, ):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page - max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = None
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["XXpageXX"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["PAGE"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = None
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["XXper_pageXX"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["PER_PAGE"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = None
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) and len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) != 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 1:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    return

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(None)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = None
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get(None, "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", None)
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", )
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("XXLinkXX", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("LINK", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "XXXX")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = None
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(None)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "XXnextXX" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "NEXT" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    return

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "XXsuccessXX": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "SUCCESS": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": False,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "XXpull_requestsXX": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "PULL_REQUESTS": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "XXtotal_countXX": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "TOTAL_COUNT": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGithubConnectorǁ_list_pull_requests__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pull requests de un repositorio.

        Args:
            params: Debe contener 'owner', 'repo' y opcionalmente 'state', 'per_page', 'page'
        """
        owner = params.get("owner", "")
        repo = params.get("repo", "")
        if not owner or not repo:
            return {"success": False, "error": "Parametros requeridos: owner, repo"}
        self._log_operation("list_pull_requests", f"{owner}/{repo}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("state", "head", "base", "sort", "direction", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_prs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 30)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/repos/{owner}/{repo}/pulls", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitHub API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                prs = resp.json()
                if not isinstance(prs, list) or len(prs) == 0:
                    break

                all_prs.extend(prs)

                # Check pagination via Link header
                link_header = resp.headers.get("Link", "")
                links = HttpClient.parse_link_header(link_header)
                if "next" not in links:
                    break

            return {
                "success": True,
                "pull_requests": all_prs,
                "total_count": len(all_prs),
            }

        except HTTPClientError as e:
            logger.error(f"GithubConnector list_pull_requests error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGithubConnectorǁ_search__mutmut)
    def _search(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = None
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get(None, "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", None)
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", )
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("XXqueryXX", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("QUERY", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "XXXX")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = None
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get(None, "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", None)
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", )
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("XXtypeXX", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("TYPE", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "XXrepositoriesXX")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "REPOSITORIES")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"XXsuccessXX": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"SUCCESS": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": True, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "XXerrorXX": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "ERROR": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "XXParametro requerido: queryXX"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "PARAMETRO REQUERIDO: QUERY"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation(None, f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation(f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("XXsearchXX", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("SEARCH", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:51]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = None
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "XXrepositoriesXX": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "REPOSITORIES": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "XX/search/repositoriesXX",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/SEARCH/REPOSITORIES",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "XXissuesXX": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "ISSUES": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "XX/search/issuesXX",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/SEARCH/ISSUES",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "XXcodeXX": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "CODE": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "XX/search/codeXX",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/SEARCH/CODE",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "XXusersXX": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "USERS": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "XX/search/usersXX",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/SEARCH/USERS",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = None

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(None, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, None)

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get("/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, )

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "XX/search/repositoriesXX")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/SEARCH/REPOSITORIES")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = None
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"XXqXX": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"Q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("XXsortXX", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("SORT", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "XXorderXX", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "ORDER", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "XXper_pageXX", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "PER_PAGE", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "XXpageXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "PAGE"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key not in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = None

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = None
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(None, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=None)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, )
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "XXsuccessXX": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "SUCCESS": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": True,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "XXerrorXX": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "ERROR": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXitemsXX": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "ITEMS": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get(None, []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", None),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get([]),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", ),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("XXitemsXX", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("ITEMS", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "XXtotal_countXX": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "TOTAL_COUNT": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get(None, 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", None),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get(0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", ),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("XXtotal_countXX", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("TOTAL_COUNT", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 1),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "XXincomplete_resultsXX": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "INCOMPLETE_RESULTS": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get(None, False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get(False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("XXincomplete_resultsXX", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("INCOMPLETE_RESULTS", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", True),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGithubConnectorǁ_search__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Busca en GitHub (repositorios, issues, codigo, usuarios).

        Args:
            params: Debe contener 'query' y 'type' (repositories/issues/code/users)
        """
        query = params.get("query", "")
        search_type = params.get("type", "repositories")
        if not query:
            return {"success": False, "error": "Parametro requerido: query"}
        self._log_operation("search", f"type={search_type}, query={query[:50]}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # Map search type to endpoint
        endpoint_map = {
            "repositories": "/search/repositories",
            "issues": "/search/issues",
            "code": "/search/code",
            "users": "/search/users",
        }
        endpoint = endpoint_map.get(search_type, "/search/repositories")

        query_params: dict[str, Any] = {"q": query}
        for key in ("sort", "order", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            resp = self._http.get(endpoint, params=query_params)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitHub API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "items": data.get("items", []),
                    "total_count": data.get("total_count", 0),
                    "incomplete_results": data.get("incomplete_results", False),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GithubConnector search error: {e}")
            return {"success": False, "error": str(None)}

mutants_xǁGithubConnectorǁ__init____mutmut['_mutmut_orig'] = GithubConnector.xǁGithubConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ__init____mutmut['xǁGithubConnectorǁ__init____mutmut_1'] = GithubConnector.xǁGithubConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ__init____mutmut['xǁGithubConnectorǁ__init____mutmut_2'] = GithubConnector.xǁGithubConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ__init____mutmut['xǁGithubConnectorǁ__init____mutmut_3'] = GithubConnector.xǁGithubConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ__init____mutmut['xǁGithubConnectorǁ__init____mutmut_4'] = GithubConnector.xǁGithubConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁGithubConnectorǁ_get_token__mutmut['_mutmut_orig'] = GithubConnector.xǁGithubConnectorǁ_get_token__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ_get_token__mutmut['xǁGithubConnectorǁ_get_token__mutmut_1'] = GithubConnector.xǁGithubConnectorǁ_get_token__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ_get_token__mutmut['xǁGithubConnectorǁ_get_token__mutmut_2'] = GithubConnector.xǁGithubConnectorǁ_get_token__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ_get_token__mutmut['xǁGithubConnectorǁ_get_token__mutmut_3'] = GithubConnector.xǁGithubConnectorǁ_get_token__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ_get_token__mutmut['xǁGithubConnectorǁ_get_token__mutmut_4'] = GithubConnector.xǁGithubConnectorǁ_get_token__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGithubConnectorǁ_get_token__mutmut['xǁGithubConnectorǁ_get_token__mutmut