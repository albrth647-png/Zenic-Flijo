"""
Conector QuickBooks — Contabilidad via QuickBooks Online API
===============================================================

Permite gestionar facturas, clientes, productos y reportes
contables via la API de QuickBooks Online.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁQuickbooksConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut: MutantDict = {}  # type: ignore
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut: MutantDict = {}  # type: ignore


class QuickbooksConnector(BaseConnector):
    """Conector para QuickBooks: facturacion, contabilidad y reportes."""

    name = "quickbooks"
    version = "1.0.0"
    description = "Gestiona facturas, clientes y reportes contables via QuickBooks Online"
    category = "finance_payments"
    icon = "calculator"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://quickbooks.api.intuit.com/v3/company"
        self._http: HttpClient | None = None
        self._company_id: str = ""

    def xǁQuickbooksConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://quickbooks.api.intuit.com/v3/company"
        self._http: HttpClient | None = None
        self._company_id: str = ""

    def xǁQuickbooksConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None
        self._company_id: str = ""

    def xǁQuickbooksConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://quickbooks.api.intuit.com/v3/companyXX"
        self._http: HttpClient | None = None
        self._company_id: str = ""

    def xǁQuickbooksConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://QUICKBOOKS.API.INTUIT.COM/V3/COMPANY"
        self._http: HttpClient | None = None
        self._company_id: str = ""

    def xǁQuickbooksConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://quickbooks.api.intuit.com/v3/company"
        self._http: HttpClient | None = ""
        self._company_id: str = ""

    def xǁQuickbooksConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://quickbooks.api.intuit.com/v3/company"
        self._http: HttpClient | None = None
        self._company_id: str = None

    def xǁQuickbooksConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://quickbooks.api.intuit.com/v3/company"
        self._http: HttpClient | None = None
        self._company_id: str = "XXXX"

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXQuickbooksConnector: credenciales OAuth2 no configuradasXX")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("quickbooksconnector: credenciales oauth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QUICKBOOKSCONNECTOR: CREDENCIALES OAUTH2 NO CONFIGURADAS")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return True
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = None
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = None
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(None)
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get(None, creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", None))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get(creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", ))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("XXcompany_idXX", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("COMPANY_ID", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get(None, "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", None)))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", )))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("XXrealm_idXX", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("REALM_ID", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "XXXX")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = None

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get(None, False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", None)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get(False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", )

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("XXsandboxXX", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("SANDBOX", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", True)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = None

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "XXhttps://sandbox-quickbooks.api.intuit.com/v3/companyXX"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "HTTPS://SANDBOX-QUICKBOOKS.API.INTUIT.COM/V3/COMPANY"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = None

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=None,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = None
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get(None, "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", None)
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", )
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("XXaccess_tokenXX", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("ACCESS_TOKEN", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "XXXX")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(None, token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=None)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", )

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("XXBearerXX", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("BEARER", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = None
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(None)
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = None
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = False
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation(None, "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", None)
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", )
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("XXconnectXX", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("CONNECT", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "XXOAuth2 configurado y validado para QuickBooksXX")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "oauth2 configurado y validado para quickbooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAUTH2 CONFIGURADO Y VALIDADO PARA QUICKBOOKS")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return False
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = None
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = False
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation(None, f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", None)
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation(f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", )
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("XXconnectXX", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("CONNECT", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return False
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = None
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = False
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation(None, "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", None)
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", )
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("XXconnectXX", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("CONNECT", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "XXOAuth2 configurado para QuickBooks (sin company_id)XX")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "oauth2 configurado para quickbooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAUTH2 CONFIGURADO PARA QUICKBOOKS (SIN COMPANY_ID)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return False
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(None)
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = None
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = None
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(None)
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get(None, creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", None))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get(creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", ))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("XXcompany_idXX", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("COMPANY_ID", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get(None, "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", None)))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", )))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("XXrealm_idXX", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("REALM_ID", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_107(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "XXXX")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_108(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = None
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_109(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get(None, False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_110(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", None)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_111(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get(False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_112(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", )
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_113(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("XXsandboxXX", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_114(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("SANDBOX", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_115(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", True)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_116(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = None
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_117(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "XXhttps://sandbox-quickbooks.api.intuit.com/v3/companyXX"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_118(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "HTTPS://SANDBOX-QUICKBOOKS.API.INTUIT.COM/V3/COMPANY"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_119(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = None
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_120(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_121(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=None,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_122(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_123(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_124(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = None
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_125(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get(None, "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_126(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", None)
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_127(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_128(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", )
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_129(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("XXaccess_tokenXX", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_130(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("ACCESS_TOKEN", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_131(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "XXXX")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_132(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(None, token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_133(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=None)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_134(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_135(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", )
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_136(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("XXBearerXX", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_137(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_138(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("BEARER", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_139(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = None
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_140(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = False
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_141(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation(None, f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_142(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", None)
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_143(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation(f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_144(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", )
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_145(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("XXconnectXX", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_146(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("CONNECT", f"OAuth2 configurado (validacion fallo: {e})")
            return True

    def xǁQuickbooksConnectorǁconnect__mutmut_147(self) -> bool:
        """Establece conexion con la API de QuickBooks Online."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("QuickbooksConnector: credenciales OAuth2 no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)

            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"

            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )

            # QuickBooks uses OAuth2 Bearer token
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Validate by querying company info
            if self._company_id:
                resp = self._http.get(f"/{self._company_id}/companyinfo/{self._company_id}")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "OAuth2 configurado y validado para QuickBooks")
                    return True
                else:
                    # Still connect even if validation fails
                    self._connected = True
                    self._log_operation("connect", f"OAuth2 configurado (validacion fallo: HTTP {resp.status_code})")
                    return True
            else:
                self._connected = True
                self._log_operation("connect", "OAuth2 configurado para QuickBooks (sin company_id)")
                return True
        except HTTPClientError as e:
            logger.warning(f"QuickbooksConnector: error durante conexion - {e}")
            creds = self._auth_provider.get_credentials()
            self._company_id = str(creds.get("company_id", creds.get("realm_id", "")))
            sandbox = creds.get("sandbox", False)
            if sandbox:
                self._base_url = "https://sandbox-quickbooks.api.intuit.com/v3/company"
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (validacion fallo: {e})")
            return False

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXcreate_invoiceXX": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "CREATE_INVOICE": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "XXlist_invoicesXX": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "LIST_INVOICES": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "XXcreate_customerXX": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "CREATE_CUSTOMER": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "XXlist_customersXX": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "LIST_CUSTOMERS": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "XXcreate_paymentXX": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "CREATE_PAYMENT": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "XXget_reportXX": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "GET_REPORT": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁQuickbooksConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector QuickBooks.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_invoice": self._create_invoice,
            "list_invoices": self._list_invoices,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_payment": self._create_payment,
            "get_report": self._get_report,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales de QuickBooks esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁQuickbooksConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales de QuickBooks esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁQuickbooksConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales de QuickBooks esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁQuickbooksConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales de QuickBooks esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁQuickbooksConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con QuickBooks."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut)
    def _create_invoice(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = None
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get(None, "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", None)
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", )
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("XXcustomer_idXX", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("CUSTOMER_ID", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "XXXX")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = None
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get(None, [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", None)
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get([])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", )
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("XXline_itemsXX", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("LINE_ITEMS", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id and not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"SUCCESS": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": True, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "XXerrorXX": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "ERROR": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "XXParametros requeridos: customer_id, line_itemsXX"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CUSTOMER_ID, LINE_ITEMS"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation(None, f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", None)

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation(f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", )

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("XXcreate_invoiceXX", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("CREATE_INVOICE", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = None
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = None
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "XXAmountXX": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "AMOUNT": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get(None, 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", None),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get(0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", ),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("XXamountXX", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("AMOUNT", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 1),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "XXDetailTypeXX": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "detailtype": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DETAILTYPE": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get(None, "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", None),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", ),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("XXdetail_typeXX", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("DETAIL_TYPE", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "XXSalesItemLineDetailXX"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "salesitemlinedetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SALESITEMLINEDETAIL"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "XXSalesItemLineDetailXX": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "salesitemlinedetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SALESITEMLINEDETAIL": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "XXItemRefXX": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "itemref": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ITEMREF": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"XXvalueXX": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"VALUE": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get(None, "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", None)},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", )},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("XXitem_idXX", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("ITEM_ID", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "XXXX")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "XXQtyXX": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "QTY": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get(None, 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", None),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get(1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", ),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("XXquantityXX", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("QUANTITY", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 2),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "XXUnitPriceXX": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "unitprice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UNITPRICE": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get(None, item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", None),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get(item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", ),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("XXunit_priceXX", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("UNIT_PRICE", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get(None, 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", None)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get(0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", )),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("XXamountXX", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("AMOUNT", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 1)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get(None):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("XXdescriptionXX"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("DESCRIPTION"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = None
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["XXDescriptionXX"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["DESCRIPTION"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["XXdescriptionXX"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["DESCRIPTION"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(None)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = None
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "XXLineXX": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "LINE": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "XXCustomerRefXX": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "customerref": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CUSTOMERREF": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"XXvalueXX": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"VALUE": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(None)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get(None):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("XXbill_emailXX"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("BILL_EMAIL"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = None
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["XXBillEmailXX"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["billemail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BILLEMAIL"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"XXAddressXX": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"ADDRESS": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["XXbill_emailXX"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["BILL_EMAIL"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get(None):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("XXdue_dateXX"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("DUE_DATE"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = None
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["XXDueDateXX"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["duedate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DUEDATE"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["XXdue_dateXX"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["DUE_DATE"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get(None):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("XXsales_term_refXX"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("SALES_TERM_REF"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = None
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["XXSalesTermRefXX"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["salestermref"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SALESTERMREF"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"XXvalueXX": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"VALUE": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["XXsales_term_refXX"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["SALES_TERM_REF"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get(None):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("XXprivate_noteXX"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("PRIVATE_NOTE"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = None

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["XXPrivateNoteXX"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["privatenote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PRIVATENOTE"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["XXprivate_noteXX"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["PRIVATE_NOTE"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                None,
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=None,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers=None,
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"XXContent-TypeXX": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"content-type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"CONTENT-TYPE": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "XXapplication/jsonXX"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "APPLICATION/JSON"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = None
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() and {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = None
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get(None, data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", None)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get(data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", )
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("XXInvoiceXX", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("INVOICE", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "XXsuccessXX": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "SUCCESS": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": False,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "XXinvoice_idXX": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "INVOICE_ID": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get(None, ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", None),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get(""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("XXIdXX", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("ID", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", "XXXX"),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "XXstatusXX": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "STATUS": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get(None, "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", None),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", ),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("XXEmailStatusXX", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("emailstatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EMAILSTATUS", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "XXDraftXX"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "DRAFT"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "XXtotalXX": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "TOTAL": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get(None, 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_209(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_210(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get(0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_211(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_212(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("XXTotalAmtXX", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_213(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("totalamt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_214(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TOTALAMT", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_215(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 1.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_216(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_217(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_218(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = None
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_219(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_220(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = None
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_221(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get(None, {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_222(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", None)
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_223(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get({})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_224(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", )
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_225(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("XXFaultXX", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_226(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_227(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("FAULT", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_228(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = None
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_229(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get(None, [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_230(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", None)
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_231(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get([])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_232(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", )
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_233(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("XXErrorXX", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_234(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_235(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("ERROR", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_236(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_237(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_238(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_239(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_240(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_241(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[1].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_242(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("XXMessageXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_243(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_244(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("MESSAGE", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_245(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_246(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_247(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_248(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_249(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_250(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_251(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_252(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_253(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_254(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_255(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_invoice__mutmut_256(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una factura en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'line_items' (lista de dicts)
        """
        customer_id = params.get("customer_id", "")
        line_items = params.get("line_items", [])
        if not customer_id or not line_items:
            return {"success": False, "error": "Parametros requeridos: customer_id, line_items"}
        self._log_operation("create_invoice", f"customer={customer_id}")

        # Build QuickBooks invoice payload
        qb_lines = []
        for item in line_items:
            line: dict[str, Any] = {
                "Amount": item.get("amount", 0),
                "DetailType": item.get("detail_type", "SalesItemLineDetail"),
                "SalesItemLineDetail": {
                    "ItemRef": {"value": item.get("item_id", "")},
                    "Qty": item.get("quantity", 1),
                    "UnitPrice": item.get("unit_price", item.get("amount", 0)),
                },
            }
            if item.get("description"):
                line["Description"] = item["description"]
            qb_lines.append(line)

        payload: dict[str, Any] = {
            "Line": qb_lines,
            "CustomerRef": {"value": str(customer_id)},
        }
        if params.get("bill_email"):
            payload["BillEmail"] = {"Address": params["bill_email"]}
        if params.get("due_date"):
            payload["DueDate"] = params["due_date"]
        if params.get("sales_term_ref"):
            payload["SalesTermRef"] = {"value": params["sales_term_ref"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/invoice",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                invoice = data.get("Invoice", data)
                return {
                    "success": True,
                    "invoice_id": invoice.get("Id", ""),
                    "status": invoice.get("EmailStatus", "Draft"),
                    "total": invoice.get("TotalAmt", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut)
    def _list_invoices(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation(None)

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("XXlist_invoicesXX")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("LIST_INVOICES")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = None
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["XXSELECT * FROM InvoiceXX"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["select * from invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM INVOICE"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = None

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get(None):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("XXstatusXX"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("STATUS"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(None)
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['XXstatusXX']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['STATUS']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get(None):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("XXcustomer_idXX"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("CUSTOMER_ID"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(None)

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['XXcustomer_idXX']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['CUSTOMER_ID']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append(None)

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " - " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("XXWHERE XX" + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("where " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(None))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + "XX AND XX".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " and ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(None)
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get(None, 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', None)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get(1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', )}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('XXstart_positionXX', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('START_POSITION', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 2)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(None)

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get(None, 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', None)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get(100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', )}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('XXlimitXX', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('LIMIT', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 101)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = None

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(None)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = "XX XX".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                None,
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params=None,
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"XXqueryXX": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"QUERY": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = None
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() and {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = None
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get(None, {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", None)
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get({})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", )
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("XXQueryResponseXX", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("queryresponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QUERYRESPONSE", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = None
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get(None, [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", None)
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get([])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", )
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("XXInvoiceXX", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("INVOICE", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "XXsuccessXX": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "SUCCESS": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": False,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "XXinvoicesXX": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "INVOICES": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "XXstart_positionXX": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "START_POSITION": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get(None, 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", None),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get(1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", ),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("XXstartPositionXX", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startposition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("STARTPOSITION", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 2),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "XXmax_resultsXX": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "MAX_RESULTS": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get(None, 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get(0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("XXmaxResultsXX", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxresults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("MAXRESULTS", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = None
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = None
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get(None, {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", None)
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get({})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", )
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("XXFaultXX", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("FAULT", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = None
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get(None, [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", None)
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get([])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", )
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("XXErrorXX", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("ERROR", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[1].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("XXMessageXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("MESSAGE", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_invoices__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista facturas de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'status', 'start_position'
        """
        self._log_operation("list_invoices")

        query_parts: list[str] = ["SELECT * FROM Invoice"]
        conditions: list[str] = []

        if params.get("status"):
            conditions.append(f"EmailStatus = '{params['status']}'")
        if params.get("customer_id"):
            conditions.append(f"CustomerRef = '{params['customer_id']}'")

        if conditions:
            query_parts.append("WHERE " + " AND ".join(conditions))

        query_parts.append(f"STARTPOSITION {params.get('start_position', 1)}")
        query_parts.append(f"MAXRESULTS {params.get('limit', 100)}")

        query = " ".join(query_parts)

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                invoices = query_response.get("Invoice", [])
                return {
                    "success": True,
                    "invoices": invoices,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut)
    def _create_customer(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = None
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get(None, "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", None)
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", )
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("XXdisplay_nameXX", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("DISPLAY_NAME", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "XXXX")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"XXsuccessXX": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"SUCCESS": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": True, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "XXerrorXX": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "ERROR": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "XXParametro requerido: display_nameXX"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "PARAMETRO REQUERIDO: DISPLAY_NAME"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation(None, f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", None)

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation(f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", )

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("XXcreate_customerXX", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("CREATE_CUSTOMER", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = None
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "XXDisplayNameXX": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "displayname": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DISPLAYNAME": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get(None):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("XXgiven_nameXX"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("GIVEN_NAME"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = None
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["XXGivenNameXX"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["givenname"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GIVENNAME"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["XXgiven_nameXX"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["GIVEN_NAME"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get(None):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("XXfamily_nameXX"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("FAMILY_NAME"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = None
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["XXFamilyNameXX"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["familyname"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FAMILYNAME"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["XXfamily_nameXX"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["FAMILY_NAME"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get(None):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("XXemailXX"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("EMAIL"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = None
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["XXPrimaryEmailAddrXX"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["primaryemailaddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PRIMARYEMAILADDR"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"XXAddressXX": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"ADDRESS": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["XXemailXX"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["EMAIL"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get(None):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("XXphoneXX"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("PHONE"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = None
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["XXPrimaryPhoneXX"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["primaryphone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PRIMARYPHONE"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"XXFreeFormNumberXX": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"freeformnumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FREEFORMNUMBER": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["XXphoneXX"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["PHONE"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get(None):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("XXcompany_nameXX"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("COMPANY_NAME"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = None
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["XXCompanyNameXX"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["companyname"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["COMPANYNAME"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["XXcompany_nameXX"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["COMPANY_NAME"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get(None):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("XXbilling_addressXX"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("BILLING_ADDRESS"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = None
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["XXBillAddrXX"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["billaddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BILLADDR"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["XXbilling_addressXX"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["BILLING_ADDRESS"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get(None):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("XXnotesXX"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("NOTES"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = None

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["XXNotesXX"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["NOTES"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["XXnotesXX"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["NOTES"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                None,
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=None,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers=None,
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"XXContent-TypeXX": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"content-type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"CONTENT-TYPE": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "XXapplication/jsonXX"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "APPLICATION/JSON"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = None
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() and {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = None
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get(None, data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", None)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get(data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", )
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("XXCustomerXX", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("CUSTOMER", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "XXsuccessXX": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "SUCCESS": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": False,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "XXcustomer_idXX": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "CUSTOMER_ID": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get(None, ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", None),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get(""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("XXIdXX", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("ID", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", "XXXX"),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "XXdisplay_nameXX": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "DISPLAY_NAME": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get(None, display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get(display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("XXDisplayNameXX", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("displayname", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DISPLAYNAME", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = None
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = None
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get(None, {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", None)
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get({})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", )
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("XXFaultXX", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("FAULT", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = None
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get(None, [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", None)
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get([])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", )
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("XXErrorXX", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("ERROR", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[1].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("XXMessageXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("MESSAGE", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_customer__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en QuickBooks.

        Args:
            params: Debe contener 'display_name' y opcionalmente 'email', 'phone'
        """
        display_name = params.get("display_name", "")
        if not display_name:
            return {"success": False, "error": "Parametro requerido: display_name"}
        self._log_operation("create_customer", f"name={display_name}")

        payload: dict[str, Any] = {
            "DisplayName": display_name,
        }
        if params.get("given_name"):
            payload["GivenName"] = params["given_name"]
        if params.get("family_name"):
            payload["FamilyName"] = params["family_name"]
        if params.get("email"):
            payload["PrimaryEmailAddr"] = {"Address": params["email"]}
        if params.get("phone"):
            payload["PrimaryPhone"] = {"FreeFormNumber": params["phone"]}
        if params.get("company_name"):
            payload["CompanyName"] = params["company_name"]
        if params.get("billing_address"):
            payload["BillAddr"] = params["billing_address"]
        if params.get("notes"):
            payload["Notes"] = params["notes"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/customer",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("Customer", data)
                return {
                    "success": True,
                    "customer_id": customer.get("Id", ""),
                    "display_name": customer.get("DisplayName", display_name),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut)
    def _list_customers(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation(None)

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("XXlist_customersXX")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("LIST_CUSTOMERS")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = None
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(None)
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get(None, 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', None))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get(1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', ))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('XXstart_positionXX', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('START_POSITION', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 2))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = None
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(None)
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get(None, 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', None))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get(100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', ))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('XXlimitXX', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('LIMIT', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 101))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"XXsuccessXX": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"SUCCESS": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": True, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "XXerrorXX": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "ERROR": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "XXstart_position y limit deben ser enterosXX"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "START_POSITION Y LIMIT DEBEN SER ENTEROS"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = None
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(None, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, None)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, )
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(2, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = None

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(None, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, None)

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, )

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(2, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(None, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, None))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, ))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1001))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = None  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                None,
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params=None,
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"XXqueryXX": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"QUERY": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = None
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() and {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = None
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get(None, {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", None)
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get({})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", )
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("XXQueryResponseXX", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("queryresponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QUERYRESPONSE", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = None
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get(None, [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", None)
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get([])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", )
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("XXCustomerXX", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("CUSTOMER", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "XXsuccessXX": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "SUCCESS": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": False,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "XXcustomersXX": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "CUSTOMERS": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "XXstart_positionXX": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "START_POSITION": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get(None, 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", None),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get(1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", ),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("XXstartPositionXX", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startposition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("STARTPOSITION", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 2),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "XXmax_resultsXX": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "MAX_RESULTS": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get(None, 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get(0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("XXmaxResultsXX", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxresults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("MAXRESULTS", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = None
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = None
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get(None, {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", None)
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get({})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", )
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("XXFaultXX", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("FAULT", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = None
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get(None, [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", None)
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get([])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", )
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("XXErrorXX", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("ERROR", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[1].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("XXMessageXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("MESSAGE", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_list_customers__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de QuickBooks.

        Args:
            params: Opcionalmente 'limit', 'start_position'
        """
        self._log_operation("list_customers")

        # Validar y normalizar start_position y limit a enteros (evita SQL injection en Quickbooks Query Language).
        try:
            start_position = int(params.get('start_position', 1))
            limit = int(params.get('limit', 100))
        except (ValueError, TypeError):
            return {"success": False, "error": "start_position y limit deben ser enteros"}
        # Cotas razonables
        start_position = max(1, start_position)
        limit = max(1, min(limit, 1000))

        # Quickbooks Query Language (no SQL estándar). start_position y limit son enteros validados.
        query = f"SELECT * FROM Customer STARTPOSITION {start_position} MAXRESULTS {limit}"  # nosec B608 — enteros validados

        try:
            resp = self._http.get(
                f"/{self._company_id}/query",
                params={"query": query},
            )
            if resp.ok:
                data = resp.json() or {}
                query_response = data.get("QueryResponse", {})
                customers = query_response.get("Customer", [])
                return {
                    "success": True,
                    "customers": customers,
                    "start_position": query_response.get("startPosition", 1),
                    "max_results": query_response.get("maxResults", 0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut)
    def _create_payment(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = None
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get(None, "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", None)
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", )
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("XXcustomer_idXX", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("CUSTOMER_ID", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "XXXX")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = None
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get(None, 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", None)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get(0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", )
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("XXtotal_amountXX", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("TOTAL_AMOUNT", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 1)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id and not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"SUCCESS": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": True, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "XXerrorXX": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "ERROR": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "XXParametros requeridos: customer_id, total_amountXX"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CUSTOMER_ID, TOTAL_AMOUNT"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation(None, f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", None)

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation(f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", )

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("XXcreate_paymentXX", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("CREATE_PAYMENT", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = None

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "XXCustomerRefXX": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "customerref": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CUSTOMERREF": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"XXvalueXX": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"VALUE": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(None)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "XXTotalAmtXX": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "totalamt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TOTALAMT": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(None),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get(None):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("XXinvoice_idXX"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("INVOICE_ID"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = None

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["XXLineXX"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["LINE"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "XXAmountXX": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "AMOUNT": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(None),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "XXLinkedTxnXX": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "linkedtxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LINKEDTXN": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "XXTxnIdXX": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "txnid": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TXNID": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(None),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["XXinvoice_idXX"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["INVOICE_ID"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "XXTxnTypeXX": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "txntype": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TXNTYPE": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "XXInvoiceXX",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "INVOICE",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get(None):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("XXpayment_methodXX"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("PAYMENT_METHOD"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = None
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["XXPaymentMethodRefXX"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["paymentmethodref"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PAYMENTMETHODREF"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"XXvalueXX": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"VALUE": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["XXpayment_methodXX"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["PAYMENT_METHOD"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get(None):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("XXpayment_refXX"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("PAYMENT_REF"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = None
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["XXPaymentRefNumXX"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["paymentrefnum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PAYMENTREFNUM"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["XXpayment_refXX"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["PAYMENT_REF"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get(None):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("XXdeposit_to_accountXX"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("DEPOSIT_TO_ACCOUNT"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = None
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["XXDepositToAccountRefXX"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["deposittoaccountref"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DEPOSITTOACCOUNTREF"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"XXvalueXX": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"VALUE": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["XXdeposit_to_accountXX"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["DEPOSIT_TO_ACCOUNT"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get(None):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("XXprivate_noteXX"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("PRIVATE_NOTE"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = None

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["XXPrivateNoteXX"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["privatenote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PRIVATENOTE"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["XXprivate_noteXX"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["PRIVATE_NOTE"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                None,
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=None,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers=None,
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"XXContent-TypeXX": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"content-type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"CONTENT-TYPE": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "XXapplication/jsonXX"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "APPLICATION/JSON"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = None
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() and {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = None
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get(None, data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", None)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get(data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", )
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("XXPaymentXX", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("PAYMENT", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "XXsuccessXX": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "SUCCESS": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": False,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "XXpayment_idXX": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "PAYMENT_ID": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get(None, ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", None),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get(""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("XXIdXX", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("ID", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", "XXXX"),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "XXtotalXX": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "TOTAL": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get(None, total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get(total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("XXTotalAmtXX", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("totalamt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TOTALAMT", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = None
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = None
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get(None, {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", None)
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get({})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", )
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("XXFaultXX", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("FAULT", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = None
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get(None, [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", None)
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get([])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", )
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("XXErrorXX", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("ERROR", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[1].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("XXMessageXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("MESSAGE", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_create_payment__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Registra un pago en QuickBooks.

        Args:
            params: Debe contener 'customer_id', 'total_amount' y opcionalmente 'invoice_id'
        """
        customer_id = params.get("customer_id", "")
        total_amount = params.get("total_amount", 0)
        if not customer_id or not total_amount:
            return {"success": False, "error": "Parametros requeridos: customer_id, total_amount"}
        self._log_operation("create_payment", f"customer={customer_id}, amount={total_amount}")

        payload: dict[str, Any] = {
            "CustomerRef": {"value": str(customer_id)},
            "TotalAmt": str(total_amount),
        }

        # Link payment to specific invoice if provided
        if params.get("invoice_id"):
            payload["Line"] = [
                {
                    "Amount": str(total_amount),
                    "LinkedTxn": [
                        {
                            "TxnId": str(params["invoice_id"]),
                            "TxnType": "Invoice",
                        }
                    ],
                }
            ]

        if params.get("payment_method"):
            payload["PaymentMethodRef"] = {"value": params["payment_method"]}
        if params.get("payment_ref"):
            payload["PaymentRefNum"] = params["payment_ref"]
        if params.get("deposit_to_account"):
            payload["DepositToAccountRef"] = {"value": params["deposit_to_account"]}
        if params.get("private_note"):
            payload["PrivateNote"] = params["private_note"]

        try:
            resp = self._http.post(
                f"/{self._company_id}/payment",
                json=payload,
                headers={"Content-Type": "application/json"},
            )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("Payment", data)
                return {
                    "success": True,
                    "payment_id": payment.get("Id", ""),
                    "total": payment.get("TotalAmt", total_amount),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁQuickbooksConnectorǁ_get_report__mutmut)
    def _get_report(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = None
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get(None, "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", None)
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", )
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("XXreport_typeXX", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("REPORT_TYPE", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "XXXX")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"XXsuccessXX": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"SUCCESS": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": True, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "XXerrorXX": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "ERROR": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "XXParametro requerido: report_typeXX"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "PARAMETRO REQUERIDO: REPORT_TYPE"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation(None, f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", None)

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation(f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", )

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("XXget_reportXX", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("GET_REPORT", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = None
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get(None):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("XXstart_dateXX"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("START_DATE"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = None
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["XXstart_dateXX"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["START_DATE"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["XXstart_dateXX"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["START_DATE"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get(None):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("XXend_dateXX"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("END_DATE"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = None
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["XXend_dateXX"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["END_DATE"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["XXend_dateXX"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["END_DATE"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get(None):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("XXaccounting_methodXX"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("ACCOUNTING_METHOD"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = None
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["XXaccounting_methodXX"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["ACCOUNTING_METHOD"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["XXaccounting_methodXX"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["ACCOUNTING_METHOD"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get(None):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("XXsummarize_column_byXX"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("SUMMARIZE_COLUMN_BY"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = None
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["XXsummarize_column_byXX"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["SUMMARIZE_COLUMN_BY"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["XXsummarize_column_byXX"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["SUMMARIZE_COLUMN_BY"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get(None):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("XXdepartmentsXX"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("DEPARTMENTS"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = None

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["XXdepartmentsXX"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["DEPARTMENTS"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["XXdepartmentsXX"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["DEPARTMENTS"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                None,
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXreport_typeXX": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "REPORT_TYPE": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "XXheaderXX": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "HEADER": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get(None, {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", None),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get({}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", ),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("XXHeaderXX", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("HEADER", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "XXrowsXX": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "ROWS": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get(None, {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get({}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("XXRowsXX", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("ROWS", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = None
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = None
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get(None, {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", None)
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get({})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", )
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("XXFaultXX", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("FAULT", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = None
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get(None, [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", None)
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get([])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", )
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("XXErrorXX", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("ERROR", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[1].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("XXMessageXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("MESSAGE", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁQuickbooksConnectorǁ_get_report__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un reporte contable de QuickBooks.

        Args:
            params: Debe contener 'report_type' (ProfitAndLoss, BalanceSheet, CashFlow, etc.)
                    Opcionalmente 'start_date', 'end_date', 'accounting_method'
        """
        report_type = params.get("report_type", "")
        if not report_type:
            return {"success": False, "error": "Parametro requerido: report_type"}
        self._log_operation("get_report", f"type={report_type}")

        query_params: dict[str, Any] = {}
        if params.get("start_date"):
            query_params["start_date"] = params["start_date"]
        if params.get("end_date"):
            query_params["end_date"] = params["end_date"]
        if params.get("accounting_method"):
            query_params["accounting_method"] = params["accounting_method"]
        if params.get("summarize_column_by"):
            query_params["summarize_column_by"] = params["summarize_column_by"]
        if params.get("departments"):
            query_params["departments"] = params["departments"]

        try:
            resp = self._http.get(
                f"/{self._company_id}/reports/{report_type}",
                params=query_params if query_params else None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "report_type": report_type,
                    "header": data.get("Header", {}),
                    "rows": data.get("Rows", {}),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                fault = error_body.get("Fault", {})
                errors = fault.get("Error", [])
                error_msg = errors[0].get("Message", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

mutants_xǁQuickbooksConnectorǁ__init____mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ__init____mutmut['xǁQuickbooksConnectorǁ__init____mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ__init____mutmut['xǁQuickbooksConnectorǁ__init____mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ__init____mutmut['xǁQuickbooksConnectorǁ__init____mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ__init____mutmut['xǁQuickbooksConnectorǁ__init____mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ__init____mutmut['xǁQuickbooksConnectorǁ__init____mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ__init____mutmut['xǁQuickbooksConnectorǁ__init____mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁ__init____mutmut_6 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁconnect__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_23'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_24'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_25'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_26'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_27'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_28'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_29'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_30'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_31'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_32'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_33'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_34'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_35'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_36'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_37'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_37 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_38'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_38 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_39'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_39 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_40'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_40 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_41'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_41 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_42'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_42 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_43'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_43 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_44'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_44 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_45'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_45 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_46'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_46 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_47'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_47 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_48'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_48 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_49'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_49 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_50'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_50 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_51'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_51 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_52'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_52 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_53'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_53 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_54'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_54 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_55'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_55 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_56'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_56 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_57'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_57 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_58'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_58 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_59'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_59 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_60'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_60 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_61'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_61 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_62'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_62 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_63'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_63 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_64'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_64 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_65'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_65 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_66'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_66 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_67'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_67 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_68'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_68 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_69'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_69 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_70'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_70 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_71'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_71 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_72'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_72 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_73'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_73 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_74'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_74 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_75'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_75 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_76'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_76 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_77'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_77 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_78'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_78 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_79'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_79 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_80'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_80 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_81'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_81 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_82'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_82 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_83'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_83 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_84'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_84 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_85'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_85 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_86'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_86 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_87'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_87 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_88'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_88 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_89'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_89 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_90'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_90 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_91'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_91 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_92'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_92 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_93'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_93 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_94'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_94 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_95'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_95 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_96'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_96 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_97'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_97 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_98'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_98 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_99'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_99 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_100'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_100 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_101'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_101 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_102'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_102 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_103'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_103 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_104'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_104 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_105'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_105 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_106'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_106 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_107'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_107 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_108'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_108 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_109'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_109 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_110'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_110 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_111'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_111 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_112'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_112 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_113'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_113 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_114'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_114 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_115'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_115 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_116'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_116 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_117'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_117 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_118'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_118 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_119'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_119 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_120'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_120 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_121'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_121 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_122'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_122 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_123'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_123 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_124'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_124 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_125'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_125 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_126'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_126 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_127'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_127 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_128'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_128 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_129'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_129 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_130'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_130 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_131'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_131 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_132'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_132 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_133'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_133 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_134'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_134 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_135'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_135 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_136'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_136 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_137'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_137 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_138'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_138 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_139'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_139 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_140'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_140 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_141'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_141 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_142'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_142 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_143'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_143 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_144'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_144 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_145'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_145 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_146'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_146 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁconnect__mutmut['xǁQuickbooksConnectorǁconnect__mutmut_147'] = QuickbooksConnector.xǁQuickbooksConnectorǁconnect__mutmut_147 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁexecute__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁexecute__mutmut['xǁQuickbooksConnectorǁexecute__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁexecute__mutmut_22 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁvalidate__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁvalidate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁvalidate__mutmut['xǁQuickbooksConnectorǁvalidate__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁvalidate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁvalidate__mutmut['xǁQuickbooksConnectorǁvalidate__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁvalidate__mutmut_2 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['xǁQuickbooksConnectorǁdisconnect__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['xǁQuickbooksConnectorǁdisconnect__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['xǁQuickbooksConnectorǁdisconnect__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['xǁQuickbooksConnectorǁdisconnect__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['xǁQuickbooksConnectorǁdisconnect__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['xǁQuickbooksConnectorǁdisconnect__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁdisconnect__mutmut['xǁQuickbooksConnectorǁdisconnect__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁdisconnect__mutmut_7 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_22 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_23'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_23 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_24'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_24 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_25'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_25 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_26'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_26 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_27'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_27 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_28'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_28 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_29'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_29 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_30'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_30 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_31'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_31 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_32'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_32 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_33'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_33 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_34'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_34 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_35'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_35 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_36'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_36 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_37'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_37 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_38'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_38 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_39'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_39 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_40'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_40 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_41'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_41 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_42'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_42 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_43'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_43 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_44'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_44 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_45'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_45 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_46'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_46 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_47'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_47 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_48'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_48 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_49'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_49 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_50'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_50 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_51'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_51 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_52'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_52 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_53'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_53 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_54'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_54 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_55'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_55 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_56'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_56 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_57'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_57 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_58'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_58 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_59'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_59 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_60'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_60 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_61'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_61 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_62'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_62 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_63'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_63 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_64'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_64 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_65'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_65 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_66'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_66 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_67'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_67 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_68'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_68 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_69'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_69 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_70'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_70 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_71'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_71 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_72'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_72 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_73'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_73 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_74'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_74 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_75'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_75 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_76'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_76 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_77'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_77 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_78'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_78 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_79'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_79 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_80'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_80 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_81'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_81 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_82'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_82 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_83'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_83 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_84'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_84 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_85'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_85 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_86'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_86 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_87'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_87 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_88'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_88 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_89'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_89 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_90'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_90 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_91'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_91 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_92'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_92 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_93'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_93 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_94'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_94 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_95'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_95 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_96'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_96 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_97'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_97 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_98'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_98 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_99'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_99 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_100'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_100 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_101'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_101 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_102'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_102 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_103'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_103 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_104'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_104 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_105'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_105 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_106'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_106 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_107'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_107 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_108'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_108 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_109'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_109 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_110'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_110 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_111'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_111 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_112'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_112 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_113'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_113 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_114'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_114 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_115'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_115 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_116'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_116 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_117'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_117 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_118'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_118 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_119'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_119 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_120'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_120 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_121'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_121 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_122'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_122 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_123'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_123 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_124'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_124 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_125'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_125 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_126'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_126 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_127'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_127 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_128'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_128 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_129'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_129 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_130'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_130 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_131'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_131 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_132'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_132 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_133'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_133 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_134'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_134 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_135'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_135 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_136'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_136 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_137'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_137 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_138'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_138 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_139'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_139 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_140'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_140 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_141'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_141 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_142'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_142 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_143'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_143 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_144'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_144 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_145'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_145 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_146'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_146 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_147'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_147 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_148'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_148 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_149'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_149 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_150'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_150 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_151'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_151 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_152'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_152 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_153'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_153 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_154'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_154 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_155'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_155 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_156'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_156 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_157'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_157 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_158'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_158 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_159'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_159 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_160'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_160 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_161'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_161 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_162'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_162 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_163'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_163 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_164'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_164 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_165'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_165 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_166'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_166 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_167'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_167 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_168'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_168 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_169'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_169 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_170'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_170 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_171'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_171 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_172'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_172 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_173'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_173 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_174'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_174 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_175'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_175 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_176'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_176 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_177'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_177 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_178'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_178 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_179'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_179 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_180'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_180 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_181'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_181 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_182'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_182 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_183'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_183 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_184'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_184 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_185'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_185 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_186'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_186 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_187'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_187 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_188'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_188 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_189'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_189 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_190'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_190 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_191'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_191 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_192'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_192 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_193'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_193 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_194'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_194 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_195'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_195 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_196'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_196 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_197'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_197 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_198'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_198 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_199'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_199 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_200'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_200 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_201'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_201 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_202'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_202 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_203'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_203 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_204'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_204 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_205'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_205 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_206'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_206 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_207'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_207 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_208'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_208 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_209'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_209 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_210'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_210 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_211'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_211 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_212'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_212 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_213'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_213 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_214'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_214 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_215'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_215 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_216'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_216 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_217'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_217 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_218'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_218 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_219'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_219 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_220'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_220 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_221'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_221 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_222'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_222 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_223'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_223 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_224'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_224 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_225'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_225 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_226'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_226 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_227'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_227 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_228'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_228 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_229'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_229 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_230'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_230 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_231'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_231 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_232'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_232 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_233'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_233 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_234'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_234 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_235'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_235 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_236'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_236 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_237'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_237 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_238'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_238 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_239'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_239 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_240'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_240 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_241'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_241 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_242'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_242 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_243'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_243 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_244'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_244 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_245'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_245 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_246'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_246 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_247'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_247 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_248'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_248 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_249'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_249 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_250'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_250 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_251'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_251 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_252'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_252 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_253'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_253 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_254'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_254 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_255'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_255 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_invoice__mutmut['xǁQuickbooksConnectorǁ_create_invoice__mutmut_256'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_invoice__mutmut_256 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_22 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_23'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_23 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_24'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_24 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_25'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_25 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_26'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_26 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_27'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_27 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_28'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_28 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_29'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_29 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_30'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_30 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_31'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_31 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_32'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_32 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_33'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_33 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_34'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_34 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_35'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_35 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_36'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_36 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_37'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_37 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_38'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_38 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_39'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_39 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_40'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_40 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_41'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_41 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_42'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_42 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_43'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_43 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_44'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_44 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_45'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_45 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_46'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_46 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_47'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_47 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_48'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_48 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_49'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_49 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_50'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_50 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_51'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_51 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_52'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_52 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_53'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_53 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_54'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_54 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_55'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_55 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_56'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_56 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_57'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_57 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_58'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_58 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_59'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_59 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_60'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_60 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_61'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_61 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_62'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_62 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_63'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_63 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_64'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_64 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_65'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_65 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_66'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_66 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_67'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_67 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_68'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_68 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_69'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_69 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_70'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_70 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_71'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_71 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_72'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_72 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_73'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_73 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_74'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_74 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_75'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_75 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_76'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_76 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_77'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_77 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_78'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_78 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_79'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_79 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_80'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_80 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_81'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_81 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_82'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_82 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_83'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_83 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_84'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_84 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_85'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_85 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_86'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_86 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_87'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_87 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_88'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_88 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_89'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_89 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_90'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_90 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_91'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_91 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_92'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_92 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_93'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_93 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_94'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_94 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_95'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_95 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_96'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_96 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_97'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_97 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_98'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_98 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_99'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_99 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_100'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_100 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_101'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_101 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_102'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_102 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_103'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_103 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_104'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_104 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_105'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_105 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_106'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_106 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_107'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_107 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_108'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_108 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_109'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_109 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_110'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_110 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_111'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_111 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_112'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_112 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_113'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_113 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_114'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_114 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_115'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_115 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_116'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_116 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_117'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_117 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_118'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_118 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_119'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_119 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_120'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_120 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_121'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_121 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_122'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_122 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_123'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_123 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_124'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_124 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_125'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_125 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_126'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_126 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_127'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_127 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_128'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_128 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_129'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_129 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_130'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_130 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_131'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_131 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_132'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_132 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_133'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_133 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_134'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_134 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_135'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_135 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_136'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_136 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_invoices__mutmut['xǁQuickbooksConnectorǁ_list_invoices__mutmut_137'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_invoices__mutmut_137 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_22 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_23'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_23 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_24'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_24 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_25'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_25 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_26'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_26 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_27'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_27 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_28'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_28 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_29'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_29 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_30'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_30 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_31'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_31 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_32'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_32 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_33'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_33 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_34'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_34 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_35'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_35 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_36'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_36 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_37'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_37 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_38'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_38 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_39'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_39 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_40'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_40 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_41'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_41 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_42'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_42 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_43'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_43 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_44'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_44 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_45'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_45 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_46'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_46 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_47'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_47 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_48'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_48 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_49'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_49 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_50'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_50 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_51'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_51 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_52'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_52 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_53'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_53 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_54'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_54 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_55'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_55 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_56'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_56 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_57'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_57 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_58'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_58 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_59'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_59 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_60'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_60 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_61'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_61 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_62'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_62 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_63'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_63 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_64'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_64 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_65'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_65 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_66'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_66 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_67'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_67 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_68'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_68 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_69'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_69 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_70'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_70 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_71'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_71 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_72'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_72 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_73'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_73 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_74'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_74 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_75'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_75 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_76'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_76 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_77'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_77 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_78'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_78 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_79'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_79 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_80'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_80 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_81'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_81 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_82'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_82 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_83'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_83 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_84'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_84 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_85'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_85 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_86'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_86 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_87'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_87 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_88'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_88 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_89'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_89 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_90'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_90 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_91'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_91 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_92'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_92 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_93'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_93 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_94'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_94 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_95'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_95 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_96'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_96 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_97'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_97 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_98'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_98 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_99'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_99 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_100'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_100 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_101'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_101 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_102'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_102 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_103'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_103 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_104'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_104 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_105'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_105 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_106'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_106 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_107'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_107 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_108'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_108 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_109'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_109 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_110'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_110 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_111'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_111 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_112'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_112 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_113'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_113 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_114'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_114 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_115'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_115 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_116'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_116 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_117'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_117 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_118'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_118 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_119'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_119 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_120'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_120 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_121'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_121 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_122'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_122 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_123'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_123 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_124'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_124 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_125'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_125 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_126'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_126 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_127'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_127 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_128'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_128 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_129'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_129 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_130'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_130 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_131'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_131 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_132'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_132 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_133'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_133 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_134'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_134 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_135'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_135 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_136'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_136 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_137'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_137 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_138'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_138 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_139'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_139 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_140'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_140 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_141'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_141 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_142'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_142 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_143'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_143 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_144'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_144 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_145'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_145 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_146'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_146 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_147'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_147 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_148'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_148 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_149'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_149 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_150'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_150 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_151'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_151 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_152'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_152 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_153'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_153 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_154'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_154 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_155'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_155 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_156'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_156 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_157'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_157 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_158'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_158 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_159'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_159 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_160'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_160 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_161'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_161 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_162'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_162 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_163'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_163 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_164'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_164 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_165'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_165 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_166'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_166 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_167'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_167 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_168'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_168 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_169'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_169 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_170'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_170 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_171'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_171 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_172'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_172 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_173'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_173 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_174'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_174 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_175'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_175 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_176'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_176 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_177'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_177 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_178'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_178 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_179'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_179 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_180'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_180 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_customer__mutmut['xǁQuickbooksConnectorǁ_create_customer__mutmut_181'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_customer__mutmut_181 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_22 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_23'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_23 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_24'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_24 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_25'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_25 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_26'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_26 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_27'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_27 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_28'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_28 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_29'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_29 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_30'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_30 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_31'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_31 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_32'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_32 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_33'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_33 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_34'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_34 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_35'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_35 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_36'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_36 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_37'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_37 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_38'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_38 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_39'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_39 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_40'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_40 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_41'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_41 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_42'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_42 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_43'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_43 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_44'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_44 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_45'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_45 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_46'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_46 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_47'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_47 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_48'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_48 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_49'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_49 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_50'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_50 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_51'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_51 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_52'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_52 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_53'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_53 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_54'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_54 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_55'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_55 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_56'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_56 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_57'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_57 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_58'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_58 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_59'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_59 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_60'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_60 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_61'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_61 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_62'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_62 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_63'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_63 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_64'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_64 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_65'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_65 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_66'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_66 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_67'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_67 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_68'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_68 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_69'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_69 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_70'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_70 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_71'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_71 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_72'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_72 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_73'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_73 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_74'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_74 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_75'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_75 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_76'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_76 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_77'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_77 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_78'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_78 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_79'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_79 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_80'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_80 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_81'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_81 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_82'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_82 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_83'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_83 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_84'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_84 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_85'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_85 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_86'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_86 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_87'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_87 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_88'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_88 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_89'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_89 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_90'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_90 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_91'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_91 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_92'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_92 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_93'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_93 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_94'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_94 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_95'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_95 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_96'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_96 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_97'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_97 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_98'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_98 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_99'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_99 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_100'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_100 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_101'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_101 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_102'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_102 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_103'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_103 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_104'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_104 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_105'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_105 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_106'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_106 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_107'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_107 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_108'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_108 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_109'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_109 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_110'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_110 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_111'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_111 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_112'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_112 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_113'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_113 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_114'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_114 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_115'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_115 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_116'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_116 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_117'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_117 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_118'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_118 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_119'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_119 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_120'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_120 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_121'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_121 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_122'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_122 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_123'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_123 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_124'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_124 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_125'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_125 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_126'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_126 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_127'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_127 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_128'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_128 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_129'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_129 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_130'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_130 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_131'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_131 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_132'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_132 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_133'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_133 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_134'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_134 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_135'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_135 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_136'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_136 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_list_customers__mutmut['xǁQuickbooksConnectorǁ_list_customers__mutmut_137'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_list_customers__mutmut_137 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_22 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_23'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_23 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_24'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_24 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_25'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_25 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_26'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_26 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_27'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_27 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_28'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_28 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_29'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_29 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_30'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_30 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_31'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_31 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_32'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_32 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_33'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_33 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_34'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_34 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_35'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_35 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_36'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_36 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_37'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_37 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_38'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_38 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_39'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_39 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_40'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_40 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_41'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_41 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_42'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_42 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_43'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_43 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_44'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_44 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_45'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_45 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_46'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_46 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_47'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_47 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_48'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_48 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_49'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_49 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_50'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_50 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_51'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_51 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_52'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_52 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_53'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_53 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_54'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_54 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_55'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_55 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_56'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_56 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_57'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_57 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_58'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_58 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_59'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_59 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_60'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_60 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_61'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_61 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_62'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_62 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_63'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_63 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_64'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_64 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_65'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_65 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_66'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_66 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_67'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_67 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_68'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_68 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_69'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_69 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_70'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_70 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_71'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_71 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_72'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_72 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_73'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_73 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_74'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_74 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_75'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_75 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_76'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_76 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_77'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_77 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_78'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_78 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_79'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_79 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_80'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_80 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_81'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_81 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_82'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_82 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_83'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_83 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_84'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_84 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_85'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_85 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_86'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_86 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_87'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_87 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_88'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_88 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_89'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_89 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_90'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_90 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_91'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_91 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_92'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_92 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_93'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_93 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_94'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_94 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_95'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_95 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_96'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_96 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_97'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_97 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_98'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_98 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_99'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_99 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_100'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_100 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_101'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_101 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_102'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_102 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_103'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_103 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_104'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_104 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_105'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_105 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_106'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_106 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_107'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_107 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_108'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_108 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_109'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_109 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_110'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_110 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_111'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_111 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_112'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_112 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_113'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_113 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_114'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_114 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_115'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_115 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_116'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_116 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_117'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_117 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_118'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_118 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_119'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_119 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_120'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_120 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_121'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_121 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_122'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_122 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_123'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_123 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_124'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_124 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_125'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_125 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_126'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_126 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_127'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_127 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_128'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_128 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_129'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_129 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_130'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_130 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_131'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_131 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_132'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_132 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_133'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_133 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_134'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_134 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_135'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_135 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_136'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_136 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_137'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_137 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_138'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_138 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_139'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_139 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_140'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_140 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_141'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_141 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_142'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_142 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_143'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_143 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_144'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_144 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_145'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_145 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_146'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_146 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_147'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_147 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_148'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_148 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_149'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_149 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_150'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_150 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_151'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_151 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_152'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_152 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_153'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_153 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_154'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_154 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_155'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_155 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_156'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_156 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_157'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_157 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_158'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_158 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_159'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_159 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_160'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_160 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_161'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_161 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_162'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_162 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_163'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_163 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_164'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_164 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_165'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_165 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_166'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_166 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_167'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_167 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_168'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_168 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_169'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_169 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_170'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_170 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_171'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_171 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_172'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_172 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_173'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_173 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_174'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_174 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_175'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_175 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_176'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_176 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_177'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_177 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_178'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_178 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_179'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_179 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_180'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_180 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_181'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_181 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_182'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_182 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_183'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_183 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_184'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_184 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_185'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_185 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_186'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_186 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_187'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_187 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_188'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_188 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_189'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_189 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_190'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_190 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_191'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_191 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_192'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_192 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_193'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_193 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_194'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_194 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_create_payment__mutmut['xǁQuickbooksConnectorǁ_create_payment__mutmut_195'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_create_payment__mutmut_195 # type: ignore # mutmut generated

mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['_mutmut_orig'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_orig # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_1'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_1 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_2'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_2 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_3'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_3 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_4'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_4 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_5'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_5 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_6'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_6 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_7'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_7 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_8'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_8 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_9'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_9 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_10'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_10 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_11'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_11 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_12'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_12 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_13'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_13 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_14'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_14 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_15'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_15 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_16'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_16 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_17'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_17 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_18'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_18 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_19'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_19 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_20'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_20 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_21'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_21 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_22'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_22 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_23'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_23 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_24'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_24 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_25'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_25 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_26'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_26 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_27'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_27 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_28'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_28 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_29'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_29 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_30'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_30 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_31'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_31 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_32'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_32 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_33'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_33 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_34'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_34 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_35'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_35 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_36'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_36 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_37'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_37 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_38'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_38 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_39'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_39 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_40'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_40 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_41'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_41 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_42'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_42 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_43'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_43 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_44'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_44 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_45'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_45 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_46'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_46 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_47'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_47 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_48'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_48 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_49'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_49 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_50'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_50 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_51'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_51 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_52'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_52 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_53'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_53 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_54'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_54 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_55'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_55 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_56'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_56 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_57'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_57 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_58'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_58 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_59'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_59 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_60'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_60 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_61'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_61 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_62'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_62 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_63'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_63 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_64'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_64 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_65'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_65 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_66'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_66 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_67'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_67 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_68'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_68 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_69'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_69 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_70'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_70 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_71'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_71 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_72'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_72 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_73'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_73 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_74'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_74 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_75'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_75 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_76'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_76 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_77'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_77 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_78'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_78 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_79'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_79 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_80'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_80 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_81'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_81 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_82'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_82 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_83'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_83 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_84'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_84 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_85'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_85 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_86'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_86 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_87'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_87 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_88'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_88 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_89'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_89 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_90'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_90 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_91'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_91 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_92'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_92 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_93'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_93 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_94'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_94 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_95'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_95 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_96'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_96 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_97'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_97 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_98'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_98 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_99'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_99 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_100'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_100 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_101'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_101 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_102'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_102 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_103'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_103 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_104'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_104 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_105'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_105 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_106'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_106 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_107'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_107 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_108'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_108 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_109'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_109 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_110'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_110 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_111'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_111 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_112'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_112 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_113'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_113 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_114'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_114 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_115'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_115 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_116'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_116 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_117'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_117 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_118'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_118 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_119'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_119 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_120'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_120 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_121'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_121 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_122'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_122 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_123'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_123 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_124'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_124 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_125'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_125 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_126'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_126 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_127'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_127 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_128'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_128 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_129'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_129 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_130'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_130 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_131'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_131 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_132'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_132 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_133'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_133 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_134'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_134 # type: ignore # mutmut generated
mutants_xǁQuickbooksConnectorǁ_get_report__mutmut['xǁQuickbooksConnectorǁ_get_report__mutmut_135'] = QuickbooksConnector.xǁQuickbooksConnectorǁ_get_report__mutmut_135 # type: ignore # mutmut generated


QUICKBOOKS_SCHEMA = ConnectorSchema(
    name="quickbooks",
    version="1.0.0",
    description="Gestiona facturas, clientes y reportes contables via QuickBooks Online",
    category="finance_payments",
    icon="calculator",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="create_invoice", description="Crea una factura", category="write"),
        ActionDefinition(name="list_invoices", description="Lista facturas", category="read"),
        ActionDefinition(name="create_customer", description="Crea un cliente", category="write"),
        ActionDefinition(name="list_customers", description="Lista clientes", category="read"),
        ActionDefinition(name="create_payment", description="Registra un pago", category="write"),
        ActionDefinition(name="get_report", description="Obtiene reporte contable", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="oauth2", required_fields=["client_id", "client_secret", "access_token"], description="QuickBooks OAuth2")
    ],
)
