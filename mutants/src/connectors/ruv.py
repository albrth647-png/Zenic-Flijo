"""
Conector RUV Chile — Impuestos y Contribuciones via SII/RUV
==============================================================

Permite gestionar contribuciones, declaraciones y consultas
tributarias via el Servicio de Impuestos Internos (SII) de Chile.
"""

from __future__ import annotations

from typing import Any

from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema
from src.utils.logger import setup_logging

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁRuvConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁ_generar_dte__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut: MutantDict = {}  # type: ignore


class RuvConnector(BaseConnector):
    """Conector para RUV Chile: impuestos, contribuciones y declaraciones."""

    name = "ruv"
    version = "1.0.0"
    description = "Gestiona contribuciones y declaraciones tributarias via SII/RUV Chile"
    category = "latam"
    icon = "landmark"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁRuvConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.sii.cl/v1"
        self._http: HttpClient | None = None

    def xǁRuvConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.sii.cl/v1"
        self._http: HttpClient | None = None

    def xǁRuvConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁRuvConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://api.sii.cl/v1XX"
        self._http: HttpClient | None = None

    def xǁRuvConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://API.SII.CL/V1"
        self._http: HttpClient | None = None

    def xǁRuvConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.sii.cl/v1"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁRuvConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXRuvConnector: credenciales no configuradasXX")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ruvconnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RUVCONNECTOR: CREDENCIALES NO CONFIGURADAS")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return True
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = None
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=None,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = None
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = None
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get(None, "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", None)
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", )
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("XXrutXX", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("RUT", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "XXXX")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = None
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get(None, "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", None)
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", )
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("XXclaveXX", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("CLAVE", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "XXXX")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = None

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get(None, "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", None)

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", )

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("XXcertificadoXX", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("CERTIFICADO", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "XXXX")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut or clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth(None, username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=None, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=None)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth(username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, )
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("XXBasicXX", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("BASIC", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header(None, certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", None)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header(certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", )
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("XXX-CertificadoXX", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("x-certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-CERTIFICADO", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header(None, rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", None)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header(rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", )

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("XXX-RUTXX", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("x-rut", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = None
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get(None)
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("XX/autenticacion/seedXX")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/AUTENTICACION/SEED")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = None
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = False
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation(None, "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", None)
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", )
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("XXconnectXX", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("CONNECT", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "XXCredenciales SII configuradas y semilla obtenidaXX")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "credenciales sii configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "CREDENCIALES SII CONFIGURADAS Y SEMILLA OBTENIDA")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return False
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = None
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = False
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation(None, "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", None)
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", )
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("XXconnectXX", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("CONNECT", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "XXCredenciales SII configuradasXX")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "credenciales sii configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "CREDENCIALES SII CONFIGURADAS")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return False
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(None)
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = None
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=None,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = None
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = False
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation(None, f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", None)
            return True

    def xǁRuvConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation(f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", )
            return True

    def xǁRuvConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("XXconnectXX", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("CONNECT", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return True

    def xǁRuvConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con la API del SII/RUV Chile."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("RuvConnector: credenciales no configuradas")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # SII uses API key + certificate-based auth
            creds = self._auth_provider.get_credentials()
            rut = creds.get("rut", "")
            clave = creds.get("clave", "")
            certificado = creds.get("certificado", "")

            if rut and clave:
                # SII uses cookie-based session auth via Semilla (seed) mechanism
                self._http.set_auth("Basic", username=rut, password=clave)
            if certificado:
                self._http.set_header("X-Certificado", certificado)
            if rut:
                self._http.set_header("X-RUT", rut)

            # Validate by getting seed token
            try:
                resp = self._http.get("/autenticacion/seed")
                if resp.ok:
                    self._connected = True
                    self._log_operation("connect", "Credenciales SII configuradas y semilla obtenida")
                    return True
            except HTTPClientError:
                pass

            # If seed endpoint fails, still set connected (some environments block it)
            self._connected = True
            self._log_operation("connect", "Credenciales SII configuradas")
            return True
        except HTTPClientError as e:
            logger.warning(f"RuvConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            self._connected = True
            self._log_operation("connect", f"Credenciales SII configuradas (verificacion fallo: {e})")
            return False

    @_mutmut_mutated(mutants_xǁRuvConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXconsultar_contribucionesXX": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "CONSULTAR_CONTRIBUCIONES": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "XXconsultar_rolXX": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "CONSULTAR_ROL": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "XXgenerar_dteXX": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "GENERAR_DTE": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "XXconsultar_dteXX": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "CONSULTAR_DTE": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "XXobtener_estado_tributarioXX": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "OBTENER_ESTADO_TRIBUTARIO": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "XXlistar_declaracionesXX": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "LISTAR_DECLARACIONES": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁRuvConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector RUV Chile.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "consultar_contribuciones": self._consultar_contribuciones,
            "consultar_rol": self._consultar_rol,
            "generar_dte": self._generar_dte,
            "consultar_dte": self._consultar_dte,
            "obtener_estado_tributario": self._obtener_estado_tributario,
            "listar_declaraciones": self._listar_declaraciones,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁRuvConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales del SII esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁRuvConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales del SII esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁRuvConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales del SII esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁRuvConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales del SII esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁRuvConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁRuvConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con SII/RUV Chile."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut)
    def _consultar_contribuciones(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = None
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get(None, "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", None)
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", )
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("XXrolXX", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("ROL", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "XXXX")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = None
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get(None, "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", None)
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", )
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("XXcomunaXX", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("COMUNA", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "XXXX")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol and not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"SUCCESS": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": True, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "XXerrorXX": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "ERROR": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "XXParametros requeridos: rol, comunaXX"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: ROL, COMUNA"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation(None, f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", None)

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation(f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", )

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("XXconsultar_contribucionesXX", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("CONSULTAR_CONTRIBUCIONES", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = None
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "XXrolXX": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "ROL": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "XXcomunaXX": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "COMUNA": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get(None):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("XXperiodoXX"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("PERIODO"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = None

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["XXperiodoXX"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["PERIODO"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["XXperiodoXX"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["PERIODO"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get(None, params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get(params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("XX/contribuciones/consultaXX", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/CONTRIBUCIONES/CONSULTA", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXrolXX": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "ROL": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "XXcomunaXX": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "COMUNA": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "XXcontribucionesXX": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "CONTRIBUCIONES": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get(None, []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", None),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get([]),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", ),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("XXcontribucionesXX", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("CONTRIBUCIONES", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "XXtotalXX": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "TOTAL": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get(None, 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get(0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("XXtotalXX", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("TOTAL", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 1.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(None, resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', None)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', )}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('XXmessageXX', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('MESSAGE', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_contribuciones__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta las contribuciones de un bien raiz.

        Args:
            params: Debe contener 'rol' (numero de rol) y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_contribuciones", f"rol={rol}")

        query_params: dict[str, Any] = {
            "rol": rol,
            "comuna": comuna,
        }
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]

        try:
            resp = self._http.get("/contribuciones/consulta", params=query_params)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "contribuciones": data.get("contribuciones", []),
                    "total": data.get("total", 0.0),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁRuvConnectorǁ_consultar_rol__mutmut)
    def _consultar_rol(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = None
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get(None, "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", None)
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", )
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("XXrolXX", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("ROL", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "XXXX")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = None
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get(None, "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", None)
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", )
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("XXcomunaXX", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("COMUNA", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "XXXX")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol and not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"SUCCESS": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": True, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "XXerrorXX": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "ERROR": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "XXParametros requeridos: rol, comunaXX"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: ROL, COMUNA"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation(None, f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", None)

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation(f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", )

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("XXconsultar_rolXX", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("CONSULTAR_ROL", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXrolXX": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "ROL": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "XXcomunaXX": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "COMUNA": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "XXdireccionXX": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "DIRECCION": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get(None, ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", None),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get(""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("XXdireccionXX", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("DIRECCION", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", "XXXX"),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "XXavaluoXX": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "AVALUO": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get(None, 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", None),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get(0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", ),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("XXavaluoXX", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("AVALUO", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 1),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "XXtipoXX": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "TIPO": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get(None, "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("XXtipoXX", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("TIPO", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "XXurbanoXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "URBANO"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(None, resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', None)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', )}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('XXmessageXX', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('MESSAGE', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_rol__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta informacion de un rol de bien raiz.

        Args:
            params: Debe contener 'rol' y 'comuna'
        """
        rol = params.get("rol", "")
        comuna = params.get("comuna", "")
        if not rol or not comuna:
            return {"success": False, "error": "Parametros requeridos: rol, comuna"}
        self._log_operation("consultar_rol", f"rol={rol}")

        try:
            resp = self._http.get(f"/bienes-raices/rol/{comuna}/{rol}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rol": rol,
                    "comuna": comuna,
                    "direccion": data.get("direccion", ""),
                    "avaluo": data.get("avaluo", 0),
                    "tipo": data.get("tipo", "urbano"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁRuvConnectorǁ_generar_dte__mutmut)
    def _generar_dte(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = None
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get(None, "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", None)
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", )
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("XXtipo_dteXX", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("TIPO_DTE", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "XXXX")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = None
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get(None, {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", None)
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get({})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", )
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("XXreceptorXX", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("RECEPTOR", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte and not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"SUCCESS": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": True, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "XXerrorXX": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "ERROR": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "XXParametros requeridos: tipo_dte, receptorXX"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: TIPO_DTE, RECEPTOR"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation(None, f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", None)

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation(f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", )

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("XXgenerar_dteXX", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("GENERAR_DTE", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = None
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "XXtipo_dteXX": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "TIPO_DTE": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "XXreceptorXX": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "RECEPTOR": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "XXdetallesXX": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "DETALLES": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get(None, []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", None),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get([]),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", ),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("XXdetallesXX", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("DETALLES", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "XXfecha_emisionXX": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "FECHA_EMISION": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get(None, ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", None),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get(""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("XXfecha_emisionXX", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("FECHA_EMISION", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", "XXXX"),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get(None):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("XXemisorXX"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("EMISOR"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = None
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["XXemisorXX"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["EMISOR"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["XXemisorXX"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["EMISOR"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get(None):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("XXreferenciasXX"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("REFERENCIAS"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = None

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["XXreferenciasXX"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["REFERENCIAS"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["XXreferenciasXX"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["REFERENCIAS"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post(None, json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post(json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("XX/dte/generarXX", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/DTE/GENERAR", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXfolioXX": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "FOLIO": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get(None, 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", None),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get(0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", ),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("XXfolioXX", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("FOLIO", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 1),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "XXtipo_dteXX": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "TIPO_DTE": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "XXestadoXX": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "ESTADO": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get(None, "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", None),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", ),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("XXestadoXX", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("ESTADO", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "XXgeneradoXX"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "GENERADO"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "XXxmlXX": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "XML": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get(None, ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get(""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("XXxmlXX", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("XML", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", "XXXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(None, resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', None)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', )}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('XXmessageXX', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('MESSAGE', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_generar_dte__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un Documento Tributario Electronico (DTE).

        Args:
            params: Debe contener 'tipo_dte', 'receptor', 'detalles', 'fecha_emision'
        """
        tipo_dte = params.get("tipo_dte", "")
        receptor = params.get("receptor", {})
        if not tipo_dte or not receptor:
            return {"success": False, "error": "Parametros requeridos: tipo_dte, receptor"}
        self._log_operation("generar_dte", f"tipo={tipo_dte}")

        payload: dict[str, Any] = {
            "tipo_dte": tipo_dte,
            "receptor": receptor,
            "detalles": params.get("detalles", []),
            "fecha_emision": params.get("fecha_emision", ""),
        }
        if params.get("emisor"):
            payload["emisor"] = params["emisor"]
        if params.get("referencias"):
            payload["referencias"] = params["referencias"]

        try:
            resp = self._http.post("/dte/generar", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": data.get("folio", 0),
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "generado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁRuvConnectorǁ_consultar_dte__mutmut)
    def _consultar_dte(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = None
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get(None, "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", None)
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", )
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("XXfolioXX", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("FOLIO", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "XXXX")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = None
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get(None, "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", None)
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", )
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("XXtipo_dteXX", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("TIPO_DTE", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "XXXX")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio and not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"SUCCESS": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": True, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "XXerrorXX": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "ERROR": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "XXParametros requeridos: folio, tipo_dteXX"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: FOLIO, TIPO_DTE"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation(None, f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", None)

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation(f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", )

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("XXconsultar_dteXX", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("CONSULTAR_DTE", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXfolioXX": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "FOLIO": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "XXtipo_dteXX": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "TIPO_DTE": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "XXestadoXX": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "ESTADO": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get(None, "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", None),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", ),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("XXestadoXX", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("ESTADO", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "XXaceptadoXX"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "ACEPTADO"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "XXxmlXX": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "XML": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get(None, ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get(""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("XXxmlXX", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("XML", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", "XXXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(None, resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', None)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', )}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('XXmessageXX', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('MESSAGE', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_consultar_dte__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta un DTE por su folio y tipo.

        Args:
            params: Debe contener 'folio' y 'tipo_dte'
        """
        folio = params.get("folio", "")
        tipo_dte = params.get("tipo_dte", "")
        if not folio or not tipo_dte:
            return {"success": False, "error": "Parametros requeridos: folio, tipo_dte"}
        self._log_operation("consultar_dte", f"folio={folio}")

        try:
            resp = self._http.get(f"/dte/consulta/{tipo_dte}/{folio}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "folio": folio,
                    "tipo_dte": tipo_dte,
                    "estado": data.get("estado", "aceptado"),
                    "xml": data.get("xml", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut)
    def _obtener_estado_tributario(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = None
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get(None, "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", None)
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", )
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("XXrutXX", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("RUT", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "XXXX")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"XXsuccessXX": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"SUCCESS": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": True, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "XXerrorXX": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "ERROR": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "XXParametro requerido: rutXX"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "PARAMETRO REQUERIDO: RUT"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation(None, f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", None)

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation(f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", )

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("XXobtener_estado_tributarioXX", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("OBTENER_ESTADO_TRIBUTARIO", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXrutXX": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "RUT": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "XXnombreXX": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "NOMBRE": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get(None, ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", None),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get(""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("XXnombreXX", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("NOMBRE", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", "XXXX"),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "XXestadoXX": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "ESTADO": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get(None, "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", None),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", ),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("XXestadoXX", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("ESTADO", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "XXACTIVOXX"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "activo"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "XXactividadesXX": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "ACTIVIDADES": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get(None, []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", None),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get([]),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", ),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("XXactividadesXX", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("ACTIVIDADES", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "XXfecha_inicioXX": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "FECHA_INICIO": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get(None, ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get(""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("XXfecha_inicioXX", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("FECHA_INICIO", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", "XXXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(None, resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', None)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', )}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('XXmessageXX', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('MESSAGE', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene el estado tributario de un contribuyente.

        Args:
            params: Debe contener 'rut'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("obtener_estado_tributario", f"rut={rut}")

        try:
            resp = self._http.get(f"/contribuyentes/estado/{rut}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "nombre": data.get("nombre", ""),
                    "estado": data.get("estado", "ACTIVO"),
                    "actividades": data.get("actividades", []),
                    "fecha_inicio": data.get("fecha_inicio", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut)
    def _listar_declaraciones(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = None
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get(None, "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", None)
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", )
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("XXrutXX", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("RUT", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "XXXX")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"XXsuccessXX": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"SUCCESS": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": True, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "XXerrorXX": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "ERROR": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "XXParametro requerido: rutXX"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "PARAMETRO REQUERIDO: RUT"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation(None, f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", None)

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation(f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", )

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("XXlistar_declaracionesXX", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("LISTAR_DECLARACIONES", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = None
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get(None):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("XXperiodoXX"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("PERIODO"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = None
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["XXperiodoXX"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["PERIODO"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["XXperiodoXX"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["PERIODO"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get(None):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("XXtipo_impuestoXX"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("TIPO_IMPUESTO"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = None
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["XXtipo_impuestoXX"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["TIPO_IMPUESTO"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["XXtipo_impuestoXX"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["TIPO_IMPUESTO"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get(None):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("XXlimitXX"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("LIMIT"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = None
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["XXlimitXX"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["LIMIT"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["XXlimitXX"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["LIMIT"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get(None):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("XXoffsetXX"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("OFFSET"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = None

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["XXoffsetXX"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["OFFSET"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["XXoffsetXX"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["OFFSET"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(None, params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXrutXX": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "RUT": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "XXdeclaracionesXX": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "DECLARACIONES": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get(None, data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get(data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("XXdeclaracionesXX", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("DECLARACIONES", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(None, resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', None)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get(resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', )}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('XXmessageXX', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('MESSAGE', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁRuvConnectorǁ_listar_declaraciones__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista las declaraciones de impuestos de un contribuyente.

        Args:
            params: Debe contener 'rut' y opcionalmente 'periodo', 'tipo_impuesto'
        """
        rut = params.get("rut", "")
        if not rut:
            return {"success": False, "error": "Parametro requerido: rut"}
        self._log_operation("listar_declaraciones", f"rut={rut}")

        query_params: dict[str, Any] = {}
        if params.get("periodo"):
            query_params["periodo"] = params["periodo"]
        if params.get("tipo_impuesto"):
            query_params["tipo_impuesto"] = params["tipo_impuesto"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("offset"):
            query_params["offset"] = params["offset"]

        try:
            resp = self._http.get(f"/declaraciones/{rut}", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "rut": rut,
                    "declaraciones": data.get("declaraciones", data if isinstance(data, list) else []),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_body.get('message', resp.body)}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

mutants_xǁRuvConnectorǁ__init____mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ__init____mutmut['xǁRuvConnectorǁ__init____mutmut_1'] = RuvConnector.xǁRuvConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ__init____mutmut['xǁRuvConnectorǁ__init____mutmut_2'] = RuvConnector.xǁRuvConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ__init____mutmut['xǁRuvConnectorǁ__init____mutmut_3'] = RuvConnector.xǁRuvConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ__init____mutmut['xǁRuvConnectorǁ__init____mutmut_4'] = RuvConnector.xǁRuvConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁconnect__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_1'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_2'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_3'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_4'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_5'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_6'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_7'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_8'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_9'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_10'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_11'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_12'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_13'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_14'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_15'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_16'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_17'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_18'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_19'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_20'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_21'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_22'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_23'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_24'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_25'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_26'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_27'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_28'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_29'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_30'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_31'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_32'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_33'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_34'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_35'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_36'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_37'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_38'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_39'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_40'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_41'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_42'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_43'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_43 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_44'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_44 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_45'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_45 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_46'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_46 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_47'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_47 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_48'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_48 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_49'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_49 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_50'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_50 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_51'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_51 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_52'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_52 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_53'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_53 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_54'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_54 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_55'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_55 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_56'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_56 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_57'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_57 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_58'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_58 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_59'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_59 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_60'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_60 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_61'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_61 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_62'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_62 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_63'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_63 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_64'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_64 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_65'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_65 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_66'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_66 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_67'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_67 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_68'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_68 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_69'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_69 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_70'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_70 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_71'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_71 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_72'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_72 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_73'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_73 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_74'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_74 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_75'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_75 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_76'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_76 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_77'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_77 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_78'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_78 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_79'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_79 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_80'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_80 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_81'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_81 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_82'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_82 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_83'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_83 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_84'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_84 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_85'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_85 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_86'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_86 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_87'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_87 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_88'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_88 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_89'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_89 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_90'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_90 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_91'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_91 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_92'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_92 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_93'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_93 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_94'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_94 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_95'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_95 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_96'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_96 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_97'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_97 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_98'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_98 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_99'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_99 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_100'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_100 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_101'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_101 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_102'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_102 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_103'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_103 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁconnect__mutmut['xǁRuvConnectorǁconnect__mutmut_104'] = RuvConnector.xǁRuvConnectorǁconnect__mutmut_104 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁexecute__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_1'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_2'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_3'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_4'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_5'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_6'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_7'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_8'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_9'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_10'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_11'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_12'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_13'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_14'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_15'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_16'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_17'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_18'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_19'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_20'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_21'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁexecute__mutmut['xǁRuvConnectorǁexecute__mutmut_22'] = RuvConnector.xǁRuvConnectorǁexecute__mutmut_22 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁvalidate__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁvalidate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁvalidate__mutmut['xǁRuvConnectorǁvalidate__mutmut_1'] = RuvConnector.xǁRuvConnectorǁvalidate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁvalidate__mutmut['xǁRuvConnectorǁvalidate__mutmut_2'] = RuvConnector.xǁRuvConnectorǁvalidate__mutmut_2 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁdisconnect__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁdisconnect__mutmut['xǁRuvConnectorǁdisconnect__mutmut_1'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁdisconnect__mutmut['xǁRuvConnectorǁdisconnect__mutmut_2'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁdisconnect__mutmut['xǁRuvConnectorǁdisconnect__mutmut_3'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁdisconnect__mutmut['xǁRuvConnectorǁdisconnect__mutmut_4'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁdisconnect__mutmut['xǁRuvConnectorǁdisconnect__mutmut_5'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁdisconnect__mutmut['xǁRuvConnectorǁdisconnect__mutmut_6'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁdisconnect__mutmut['xǁRuvConnectorǁdisconnect__mutmut_7'] = RuvConnector.xǁRuvConnectorǁdisconnect__mutmut_7 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_1'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_2'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_3'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_4'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_5'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_6'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_7'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_8'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_9'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_10'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_11'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_12'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_13'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_14'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_15'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_16'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_17'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_18'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_19'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_20'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_21'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_22'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_23'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_24'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_25'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_26'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_27'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_28'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_29'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_30'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_31'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_32'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_33'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_34'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_35'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_36'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_37'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_38'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_39'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_40'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_41'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_42'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_43'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_43 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_44'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_44 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_45'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_45 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_46'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_46 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_47'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_47 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_48'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_48 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_49'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_49 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_50'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_50 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_51'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_51 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_52'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_52 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_53'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_53 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_54'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_54 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_55'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_55 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_56'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_56 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_57'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_57 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_58'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_58 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_59'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_59 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_60'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_60 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_61'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_61 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_62'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_62 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_63'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_63 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_64'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_64 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_65'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_65 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_66'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_66 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_67'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_67 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_68'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_68 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_69'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_69 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_70'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_70 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_71'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_71 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_72'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_72 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_73'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_73 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_74'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_74 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_75'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_75 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_76'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_76 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_77'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_77 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_78'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_78 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_79'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_79 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_80'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_80 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_81'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_81 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_82'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_82 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_83'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_83 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_84'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_84 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_85'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_85 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_86'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_86 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_87'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_87 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_88'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_88 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_89'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_89 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_90'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_90 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_91'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_91 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_92'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_92 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_93'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_93 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_94'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_94 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_95'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_95 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_96'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_96 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_97'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_97 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_98'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_98 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_contribuciones__mutmut['xǁRuvConnectorǁ_consultar_contribuciones__mutmut_99'] = RuvConnector.xǁRuvConnectorǁ_consultar_contribuciones__mutmut_99 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_1'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_2'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_3'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_4'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_5'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_6'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_7'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_8'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_9'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_10'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_11'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_12'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_13'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_14'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_15'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_16'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_17'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_18'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_19'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_20'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_21'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_22'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_23'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_24'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_25'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_26'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_27'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_28'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_29'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_30'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_31'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_32'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_33'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_34'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_35'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_36'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_37'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_38'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_39'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_40'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_41'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_42'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_43'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_43 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_44'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_44 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_45'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_45 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_46'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_46 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_47'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_47 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_48'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_48 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_49'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_49 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_50'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_50 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_51'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_51 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_52'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_52 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_53'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_53 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_54'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_54 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_55'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_55 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_56'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_56 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_57'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_57 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_58'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_58 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_59'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_59 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_60'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_60 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_61'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_61 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_62'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_62 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_63'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_63 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_64'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_64 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_65'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_65 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_66'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_66 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_67'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_67 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_68'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_68 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_69'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_69 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_70'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_70 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_71'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_71 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_72'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_72 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_73'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_73 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_74'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_74 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_75'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_75 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_76'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_76 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_77'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_77 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_78'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_78 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_79'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_79 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_80'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_80 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_81'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_81 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_82'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_82 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_83'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_83 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_84'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_84 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_85'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_85 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_86'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_86 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_87'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_87 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_88'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_88 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_89'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_89 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_90'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_90 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_91'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_91 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_rol__mutmut['xǁRuvConnectorǁ_consultar_rol__mutmut_92'] = RuvConnector.xǁRuvConnectorǁ_consultar_rol__mutmut_92 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁ_generar_dte__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_1'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_2'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_3'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_4'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_5'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_6'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_7'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_8'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_9'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_10'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_11'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_12'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_13'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_14'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_15'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_16'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_17'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_18'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_19'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_20'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_21'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_22'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_23'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_24'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_25'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_26'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_27'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_28'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_29'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_30'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_31'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_32'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_33'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_34'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_35'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_36'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_37'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_38'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_39'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_40'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_41'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_42'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_43'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_43 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_44'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_44 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_45'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_45 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_46'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_46 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_47'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_47 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_48'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_48 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_49'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_49 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_50'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_50 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_51'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_51 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_52'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_52 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_53'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_53 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_54'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_54 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_55'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_55 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_56'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_56 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_57'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_57 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_58'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_58 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_59'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_59 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_60'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_60 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_61'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_61 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_62'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_62 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_63'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_63 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_64'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_64 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_65'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_65 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_66'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_66 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_67'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_67 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_68'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_68 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_69'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_69 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_70'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_70 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_71'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_71 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_72'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_72 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_73'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_73 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_74'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_74 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_75'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_75 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_76'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_76 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_77'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_77 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_78'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_78 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_79'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_79 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_80'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_80 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_81'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_81 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_82'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_82 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_83'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_83 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_84'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_84 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_85'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_85 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_86'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_86 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_87'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_87 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_88'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_88 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_89'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_89 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_90'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_90 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_91'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_91 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_92'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_92 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_93'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_93 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_94'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_94 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_95'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_95 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_96'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_96 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_97'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_97 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_98'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_98 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_99'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_99 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_100'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_100 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_101'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_101 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_102'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_102 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_103'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_103 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_104'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_104 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_105'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_105 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_106'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_106 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_107'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_107 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_108'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_108 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_109'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_109 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_110'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_110 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_111'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_111 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_112'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_112 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_113'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_113 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_114'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_114 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_115'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_115 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_116'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_116 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_117'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_117 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_118'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_118 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_119'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_119 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_120'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_120 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_121'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_121 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_122'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_122 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_123'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_123 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_124'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_124 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_125'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_125 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_126'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_126 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_127'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_127 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_128'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_128 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_129'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_129 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_130'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_130 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_131'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_131 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_generar_dte__mutmut['xǁRuvConnectorǁ_generar_dte__mutmut_132'] = RuvConnector.xǁRuvConnectorǁ_generar_dte__mutmut_132 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_1'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_2'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_3'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_4'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_5'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_6'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_7'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_8'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_9'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_10'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_11'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_12'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_13'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_14'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_15'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_16'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_17'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_18'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_19'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_20'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_21'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_22'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_23'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_24'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_25'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_26'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_27'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_28'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_29'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_30'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_31'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_32'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_33'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_34'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_35'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_36'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_37'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_38'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_39'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_40'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_41'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_42'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_43'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_43 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_44'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_44 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_45'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_45 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_46'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_46 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_47'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_47 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_48'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_48 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_49'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_49 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_50'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_50 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_51'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_51 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_52'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_52 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_53'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_53 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_54'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_54 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_55'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_55 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_56'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_56 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_57'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_57 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_58'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_58 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_59'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_59 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_60'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_60 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_61'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_61 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_62'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_62 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_63'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_63 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_64'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_64 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_65'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_65 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_66'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_66 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_67'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_67 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_68'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_68 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_69'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_69 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_70'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_70 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_71'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_71 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_72'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_72 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_73'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_73 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_74'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_74 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_75'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_75 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_76'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_76 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_77'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_77 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_78'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_78 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_79'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_79 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_80'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_80 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_81'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_81 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_82'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_82 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_consultar_dte__mutmut['xǁRuvConnectorǁ_consultar_dte__mutmut_83'] = RuvConnector.xǁRuvConnectorǁ_consultar_dte__mutmut_83 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_1'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_2'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_3'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_4'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_5'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_6'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_7'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_8'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_9'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_10'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_11'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_12'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_13'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_14'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_15'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_16'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_17'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_18'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_19'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_20'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_21'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_22'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_23'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_24'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_25'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_26'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_27'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_28'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_29'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_30'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_31'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_32'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_33'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_34'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_35'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_36'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_37'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_38'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_39'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_40'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_41'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_42'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_43'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_43 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_44'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_44 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_45'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_45 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_46'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_46 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_47'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_47 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_48'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_48 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_49'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_49 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_50'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_50 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_51'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_51 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_52'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_52 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_53'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_53 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_54'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_54 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_55'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_55 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_56'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_56 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_57'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_57 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_58'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_58 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_59'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_59 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_60'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_60 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_61'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_61 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_62'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_62 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_63'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_63 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_64'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_64 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_65'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_65 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_66'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_66 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_67'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_67 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_68'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_68 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_69'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_69 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_70'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_70 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_71'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_71 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_72'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_72 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_73'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_73 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_74'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_74 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_75'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_75 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_76'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_76 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_77'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_77 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_78'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_78 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_79'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_79 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_80'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_80 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_81'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_81 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_82'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_82 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_83'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_83 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_84'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_84 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_85'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_85 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_86'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_86 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_87'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_87 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_obtener_estado_tributario__mutmut['xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_88'] = RuvConnector.xǁRuvConnectorǁ_obtener_estado_tributario__mutmut_88 # type: ignore # mutmut generated

mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['_mutmut_orig'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_1'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_2'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_3'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_4'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_5'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_6'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_7'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_8'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_9'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_10'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_11'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_12'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_13'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_14'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_15'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_16'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_17'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_18'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_19'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_20'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_21'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_22'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_23'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_24'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_25'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_26'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_27'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_28'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_29'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_30'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_31'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_32'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_33'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_34'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_35'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_36'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_37'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_38'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_39'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_40'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_41'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_42'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_43'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_43 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_44'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_44 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_45'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_45 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_46'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_46 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_47'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_47 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_48'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_48 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_49'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_49 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_50'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_50 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_51'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_51 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_52'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_52 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_53'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_53 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_54'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_54 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_55'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_55 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_56'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_56 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_57'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_57 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_58'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_58 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_59'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_59 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_60'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_60 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_61'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_61 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_62'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_62 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_63'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_63 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_64'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_64 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_65'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_65 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_66'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_66 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_67'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_67 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_68'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_68 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_69'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_69 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_70'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_70 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_71'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_71 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_72'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_72 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_73'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_73 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_74'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_74 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_75'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_75 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_76'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_76 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_77'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_77 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_78'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_78 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_79'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_79 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_80'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_80 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_81'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_81 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_82'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_82 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_83'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_83 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_84'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_84 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_85'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_85 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_86'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_86 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_87'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_87 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_88'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_88 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_89'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_89 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_90'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_90 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_91'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_91 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_92'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_92 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_93'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_93 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_94'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_94 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_95'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_95 # type: ignore # mutmut generated
mutants_xǁRuvConnectorǁ_listar_declaraciones__mutmut['xǁRuvConnectorǁ_listar_declaraciones__mutmut_96'] = RuvConnector.xǁRuvConnectorǁ_listar_declaraciones__mutmut_96 # type: ignore # mutmut generated


RUV_SCHEMA = ConnectorSchema(
    name="ruv",
    version="1.0.0",
    description="Gestiona contribuciones y declaraciones tributarias via SII/RUV Chile",
    category="latam",
    icon="landmark",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="consultar_contribuciones", description="Consulta contribuciones", category="read"),
        ActionDefinition(name="consultar_rol", description="Consulta informacion de rol", category="read"),
        ActionDefinition(name="generar_dte", description="Genera un DTE", category="write"),
        ActionDefinition(name="consultar_dte", description="Consulta un DTE", category="read"),
        ActionDefinition(name="obtener_estado_tributario", description="Obtiene estado tributario", category="read"),
        ActionDefinition(name="listar_declaraciones", description="Lista declaraciones", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="api_key", required_fields=["rut", "certificado", "clave"], description="RUT + Certificado Digital SII Chile")
    ],
)
